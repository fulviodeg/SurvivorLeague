# Current status

> **Role:** Live status of the Survivor League project (phase, work plan, checkpoints, next step). External file referenced from `AGENTS.md` §1.7.
> **When to update:** after every substantial change (see `AGENTS.md` Part 2 §0); bump `Last updated` and append a dated changelog entry.
> **Last updated:** 2026-08-30T22:50:00Z

- **Phase:** POC under implementation — design aligned (**PRD v0.6.2, HLD v0.6.0, LLD v0.6.2**, ADR-001…**019**). **Feature "partecipazione opt-in — registration ≠ join" implementata (2026-08-30, branch `feat/win-only-mode`, piano `.kilo/plans/1788106316564-optin-tournament-participation.md`, working tree NON committato — nessun commit/push)**: RIMOSSO l'auto-join al primo pick (RF-P5, ADR-009 decisione 6 emendata). Due colonne additive su `platform_account` (`receive_tournament_start_notification`/`tournament_auto_join`, `INTEGER NOT NULL DEFAULT 1`, DDL + `applyPlatformAdditiveMigrations`); `PlatformRegistry` con `receiveTournamentStartNotification`/`tournamentAutoJoin`, flag scritti a true solo alla prima creazione, nuovi `activeAccounts()`/`setPreferences(email, prefs)`; CLI `platform:register --auto-join --receive-notifications` (bool default true) + nuovo `platform:preferences --email [--auto-join on|off] [--receive-notifications on|off]` + `platform:list` con i due flag; `src/game/registration.ts` con `createProfileForAccount` (unica fonte profilo), `autoJoinProfilesAtStart` (snapshot a `tournament:start`) e `declareParticipation` (esito `DeclareResult`), RIMOSSO `autoJoinFromPick`; `startTournament` con auto-join bulk + campo `autoJoined` e broadcast `tournament_open` filtrato su `receiveTournamentStartNotification` (CTA "rispondi con 'PARTECIPO'"); nuovo comando `tournament:join --email [--reason]` (override late con `--reason` obbligatorio fuori finestra TT1); nuovo intento `join` (formula `PARTECIPO`) in `intent-classifier.ts`/`deterministic-parser.ts`; ramo join in `channel:email:process` (pick da attivo senza profilo → `tournament_join_rejected` reason `not_in_tournament`, nessun auto-join); rimossa l'eccezione TT1 in `openRound` (campo `registeredNotified` rimosso da `RoundOpenResult`); simulazione con registrazione account PRIMA di `startTournament` e `autoJoinProfilesAtStart` esplicito in `simulateRound` (seed RNG cambiato, accettato RNF1); 3 nuovi `EmailType` `tournament_join_confirmed`/`tournament_already_joined`/`tournament_join_rejected` (soggetti "Partecipazione Confermata"/"Già in Gara"/"Partecipazione Non Confermata") + `clarification` con 4 opzioni. **ADR-019 registrata; FASE D (documentazione) completata** (PRD v0.6.2, HLD v0.6.0, LLD v0.6.2, cli-reference, technical-administrator-manual, system-components, guida test-mode §5.6, current-status). Suite di test verde (vedi changelog).

- **Phase:** POC under implementation — design aligned (**PRD v0.6.x, HLD v0.5.0, LLD v0.6.1**, ADR-001…**018**). **Feature JOLLY implementata e committata (2026-08-30, branch `feat/win-only-mode`, piano `.kilo/plans/1788027046413-jolly-feature.md`, TDD, revisione indipendente PASS in `docs/reviews/2026-08-30/review-jolly-feature.md`)**: `JOLLIES_PER_PLAYER` (int ≥ 0, default `1`, `0` = feature off) — in `win_only` il jolly (keyword "jolly" nel pick email, es. "Napoli Jolly", case/accenti-insensibile ovunque nel testo) salva dall'eliminazione in caso di **pareggio** (NON dalla sconfitta) ed è **bruciato alla dichiarazione**. **`GameMode { winOnly, jollyEnabled }` + `modeFor`** in `src/game/mode.ts` (oggetto estensibile, iniettato a renderer/generatori/template al posto del booleano `winOnly`); 3 colonne additive (`tournament_state.jollies_per_player`, `profile.jollies_remaining`, `pick.jolly_used`) con migrazioni idempotenti; guardia fatal estesa a `jollies_per_player`; cascata `jolly_not_allowed`/`no_jollies_left` (dopo `pick_already_exists`, prima dei check temporali) + decremento atomico del contatore (`db.transaction` in `registerPick`, BEGIN/COMMIT in auto-join); scoring `savedByJolly` (pick `wrong` su pareggio con jolly → `correct`, nessun nuovo `pick.status`, flag runtime alle email); parser deterministico + classificatore LLM jolly-aware (`PickParseOptions.jollyEnabled`); testi email jolly (chiave "PICK REGISTRATO CON JOLLY → {TEAM}", righe 🎯 per istruzioni/conferma/esiti, "🎯 Jolly rimasti: N", marcatore "🎯 Jolly" in `playerResultRow`, motivi jolly in italiano); CLI `pick:* --jolly`, `llm:*` jollyEnabled/`--jolly-used`/`--jollies-remaining`; simulazione con seed jolly (extra `rng()` gated su `jollyEnabled`, probabilità 0.25, sequenza classica invariata quando off); **ADR-018 registrata**; documentazione allineata (LLD v0.6.1, technical-administrator-manual, cli-reference, guida test-mode, system-components, current-status). **710 test verdi, typecheck/lint puliti.**

- **Phase:** POC under implementation — design aligned (**PRD v0.6.x, HLD v0.5.0, LLD v0.6.0**, ADR-001…**017**). **Rosa del calendario sintetico allineata alla Serie A 2026/27 (2026-08-30, branch `feat/win-only-mode`, working tree NON committato)**: `SYNTHETIC_TEAMS`/`SYNTHETIC_TEAM_SHORT_NAMES` (`src/data/synthetic-season.ts`) = 20 nomi canonici reali dell'API football-data.org (stagione SA 2026: AC Milan, AC Monza, ACF Fiorentina, AS Roma, Atalanta BC, Bologna FC 1909, Cagliari Calcio, Como 1907, FC Internazionale Milano, Frosinone Calcio, Genoa CFC, Juventus FC, Parma Calcio 1913, SS Lazio, SSC Napoli, Torino FC, US Lecce, US Sassuolo Calcio, Udinese Calcio, Venezia FC; shortName dall'API); risorsa `src/llm/team-aliases-synthetic.md` riscritta (20 canonici + alias, incl. Frosinone/Monza/Venezia; rimossi Pisa/Hellas Verona/Cremonese); prompt test mode in `parser.ts`/`intent-classifier.ts` → "rosa di Serie A 2026/27"; `scripts/seed-seriea-synthetic.mjs` ora importa `SYNTHETIC_TEAMS` (niente lista duplicata); test aggiornati (TDD: rosa 2026/27 in `synthetic-season.test.ts`, marker Frosinone vs Empoli in `parser.test.ts`, prompt 2026/27); documentazione allineata (guida-test-mode §1.2 roster+alias e §5.5 SQL esempio, system-prompt-agente-test-mode §3.3, technical-administrator-manual §3.5/§5, cli-reference `data:seed-synthetic`, `.env*.example`). **647 test verdi, typecheck/lint puliti.** Rosa scaricata live da football-data.org (token `.env`, `FOOTBALL_DATA_SEASON=2026`).

- **Phase:** POC under implementation — design aligned (**PRD v0.6.x, HLD v0.5.0, LLD v0.6.0**, ADR-001…**017**). **Feature AUTOPICK implementata (2026-08-30, branch `feat/win-only-mode`, piano `.kilo/plans/1788074961317-autopick-on-missing.md`, working tree NON committato — nessun commit/push)**: `AUTOPICK_ON_MISSING` (bool, default `false`, attiva SOLO con `WIN_ONLY=true`) — alla chiusura di un round con deadline REALE (`round_state.deadline !== null`) il motore assegna ai profili in gara senza pick la **prima squadra disponibile per `shortName`** (tabella `team` additiva `(name PK, short_name)`, popolata da import/synthetic con `upsertTeams`); pick auto = `insertPendingPick(..., autoPick=1)` con `outcome='win'` (bypassa `validatePick`), flag `pick.auto_pick`; colonna additiva `tournament_state.autopick_on_missing` fissata a `tournament:start` (stessa UPSERT di `win_only`) e coperta dalla STESSA guardia fatal `assertModeConsistent` (una chiave in più); nuova email `pick_auto_assigned` (soggetto `Pick Auto Assegnato`, keyMessage `PICK AUTO ASSEGNATO → {TEAM}`, NO sezione deadline) + marcatore `🤖 Auto-assegnato` in `playerResultRow`; nuovo comando `rules:teams` (ordinato per `short_name`); fallback `missing_pick` + warn se nessuna squadra disponibile (D6); deadline NULL → niente auto-assign (D3). **ADR-017 registrata; documentazione T11 completata** (HLD, LLD v0.6.0, technical-administrator-manual, cli-reference, guida test-mode, system-components, current-status). Suite di test verde (vedi changelog).
- **Phase:** POC under implementation — design aligned (**PRD v0.6.x, HLD v0.5.0, LLD v0.5.0**, ADR-001…**016**). **Modalità `win_only` implementata (2026-08-28, branch `feat/win-only-mode`, piano `.kilo/plans/1787928380301-win-only-mode.md`, working tree NON committato — nessun commit/push)**: pick = sola squadra vincente (`outcome='win'`), pareggio/sconfitta = eliminazione; `WIN_ONLY` (bool, default `true` — `win_only` è la modalità di default) fissata nel DB a `tournament:start` + guardia fatal generica `assertModeConsistent` (`src/game/mode.ts`) che abbatte il processo su cambio a torneo aperto; cascata `invalid_outcome` mode-aware; parser deterministico/classificatore LLM win_only-aware (squadra nuda = pick, draw/lose espliciti = chiarimento); overlay testi email win_only (narrative/prompt/CTA/key/righe giocatore); CLI `pick:* --outcome` opzionale senza default; simulazione con seed solo `win`; ADR-016 registrata; manuale/cli-reference/guida test-mode/LLD/system-components/current-status allineati. **619 test verdi, typecheck e lint puliti** (validazione finale in corso). Jolly = piano dedicato successivo (fuori scope qui, forward-compatible).

- **Phase:** POC under implementation — design aligned (**PRD v0.6.x, HLD v0.5.0, LLD v0.5.0**, ADR-001…**015**). **Email V3+V4 merged su `main` (2026-08-26, PR #3 squash `67588f7`): renderer deterministico, parser input, chiusura torneo con storico — branch `feat/email-v3` eliminato localmente e sul remoto** (vedi changelog). **Setup VPS per il deploy avviato (2026-08-26)**: chiave SSH dedicata `~/.ssh/id_ed25519_vps` + alias `survivor-vps` (root@147.93.123.121, fingerprint `SHA256:DxYMAws4R+KXo70Jv1qUvUPFj2/jO5llAyEfDWVV3TE` verificata e registrata in known_hosts), `ssh-copy-id` completato (accesso senza password OK, hostname `srv1822163`), file credenziali `vps-access` gitignorato; verifica prontezza host completata (Ubuntu 24.04.4 LTS x86_64, 2 core/7.8GiB, 89G liberi, cron attivo, fuso UTC, glibc 2.39, egress OK verso Gmail 993/587 + football-data.org + openrouter.ai) e **Node 22.23.2 LTS installato via NodeSource + `build-essential`** (full-icu OK). Nota: sulla VPS è presente anche Docker (repo configurato) e c'è un reboot pendente per il kernel 6.8.0-138. **Prossimo passo: piano di deploy del progetto sulla VPS** (lacune note: comando `health:check` non ancora implementato — HIGH-05/R2; in produzione il CLI gira oggi via tsx, da valutare build → `node dist/index.js`).

- **Phase:** POC under implementation — design aligned (**PRD v0.6.x, HLD v0.5.0, LLD v0.5.0**, ADR-001…**015**). **Email V4 eseguita (2026-08-25, branch `feat/email-v3`, piano `.kilo/plans/1787675020248-email-v4-chiusura-torneo.md`, working tree NON committato): header "Round del torneo N · Turno di Campionato M", elenco giocatori nel riepilogo, nomi co-vincitori, nuova mail `tournament_closed` con storico per-round** (vedi changelog). **Email V3 completa (Parte A + Parte B) eseguita (2026-08-24, branch `feat/email-v3`): restyle plain-text senza riquadri, generatore deterministico `AI_EMAIL_GENERATOR` e parser deterministico `AI_EMAIL_PARSER`** (vedi changelog). **UAT "risultati che arrivano dopo la creazione del calendario" eseguita dal vivo (2026-08-23, branch `feat/email-templates-v2`) + nuovo scenario §5.5 documentato nella guida test-mode**: sessione commissioner su DB freschi (torneo `uat-synthetic-pippo.db` pulito, platform `uat-platform-pippo.db` con i 3 account attivi intatti — Fulvio De Giovanni/Pippi/Fulvio), calendario compresso `--teams 4 --rounds 2 --spacing-min 10 --first-kickoff-offset-min 15 --seed 42`, `.env.uat` con `SCHEDULER_ENABLED=false` (commissioner). **Flusso verificato dal vivo per entrambi i round**: azzeramento punteggi `match` via `UPDATE ... SET home_score=NULL` (simula partite SCHEDULED/IN_PLAY) → `round:score` con risultati assenti lascia il round `closed` con pick `pending` (`valutati: 0`, `scored_at=null`, `summary_sent=0`, nessun riepilogo — ramo incrementale ADR-003/CL7/RF-16) → iniezione risultati via `UPDATE` (simula `data:refresh` FINISHED) → secondo `round:score` → `scored`, esiti `correct`/`wrong` + riepilogo `round_closed_survived`. Round 1: 3/3 `correct` (nessun eliminato). Round 2: 1 `wrong` (Fulvio De Giovanni eliminato) → **chiusura automatica torneo** (ADR-011): `winner_notified=1`, vincitori caso 3 `fulviodegiovanni@live.com`+`sara.zizzari@gmail.com`, export `data/exports/tournament-export-2026-08-23T17-11-52.481Z.json` (6 tabelle, turns TT/TC), scheduler "Prossime azioni: nessuna". Email reali con mistral-nemo (pick `auto_joined` al TT1, `pick_registered` al TT2). Nota: episodio "pick a round non ancora aperto" → `round_not_open` (CL3) verificato ma NON documentato in guida (già testato in passato, richiesta utente). **Documentazione**: nuova sezione `docs/uat/guida-test-mode.md` §5.5 "Scenario: risultati che arrivano DOPO la creazione del calendario" (query DB esatte per azzeramento/iniezione punteggi, output attesi verbatim, note operative) + rimandi in §6.1 (nuovo bullet "cosa si può dimostrare") e §6.2 (effetto refresh simulabile via §5.5). Solo documentazione, nessuna modifica al codice; working tree con modifiche non committate.

- **Phase:** POC under implementation — design aligned (**PRD v0.6.x, HLD v0.5.0, LLD v0.5.0**, ADR-001…**012**). **Fix dei findings della review uncommitted 2026-08-23 (piano `.kilo/plans/1787484219693-review-findings-fix.md` eseguito, branch `feat/email-templates-v2`, working tree NON committato)**: F2 `inGameCount` calcolato UNA volta dopo tutte le eliminazioni (helper `countInGame`, notify differite in closeRound/scoreRound → stesso valore su ogni email della run, coerente col riepilogo); F3 recupero EXPORT in `settleWinnerIfNeeded` (torneo chiuso con `export_path` NULL → ritenta solo l'archiviazione al rientro, mai rinotificare i vincitori; fallimento sempre loggato) + `getTournamentState` esportato da tournament.ts; F4 CTA disiscrizione derivata da `UNSUBSCRIBE_CONFIRM_WORDS` (renderer, testo identico); F5 rimossa l'iniezione morta deadline/countdown dal riepilogo `round_closed_survived`; F6 `getBurnedEmailTeams` riceve `startRound` letto una volta (niente N query per profilo); F7 scheduler usa il lettore/predicato condivisi `getTournamentState`/`isTournamentClosed` (rimosse copia locale e predicati inline); F8 bottom-posting NON fixato (decisione utente: troppo oneroso, rischio accettato); F1 eliminato `.check-inbox.mjs` (credenziali in chiaro — ROTAZIONE App Password Gmail raccomandata all'utente); F9 eliminato `templates.old.ts` (file morto) + commenti/LLD aggiornati. **526 test verdi (3 nuovi: 2 conteggi + 1 export-retry), typecheck/lint puliti.** E2E TEST MODE su DB freschi completato (vedi changelog).

- **Phase:** POC under implementation — design aligned (**PRD v0.6.x, HLD v0.5.0, LLD v0.5.0**, ADR-001…**012**). **Taglio deterministico della citazione nelle reply email (piano `.kilo/plans/1787396782368-email-reply-quote-stripping.md` eseguito 2026-08-23, branch `feat/email-templates-v2`, working tree NON committato)**: nuovo modulo puro `src/channel/email-adapter/reply-cleaner.ts` — `extractPlayerReply(body)` estrae SOLO il testo del giocatore (max 5 righe non vuote, taglio al primo confine di citazione: separatore `───` anche citato con `>` / riga `>` / marker "ha scritto:"/"wrote:" anche spezzato su due righe — wrapping reale Gmail; CRLF→LF; fallback al body originale trimmato se il taglio produrrebbe vuoto, D6) + costante condivisa `SYSTEM_EMAIL_SEPARATOR='───'` (PROVVISORIA, unico punto: invio e ricezione non possono divergere); wiring in `message-router.classify` (taglio dopo il check vuoto→unknown, D1) e in `EmailAdapter.sendMessage` (D3: separatore anteposto a OGNI email di sistema, PRIMA del banner TEST MODE). Risolve l'incidente UAT 2026-08-22 (classificatore LLM confuso dalla citazione con box bruciate/partite): accettazione verificata DAL VIVO con `llm:classify` su `ENV_FILE=.env.uat` (mistral-nemo primario) — body puliti UID 291→`US Cremonese draw`, UID 293→`US Catanzaro` win, UID 295→`US Catanzaro draw` (prima: US Catanzaro lose / SSC Bari win / unrecognized). Regressioni verbatim dei 3 body reali nei test. **523 test verdi, typecheck/lint puliti.**

- **Phase:** POC under implementation — design aligned (**PRD v0.6.x, HLD v0.5.0, LLD v0.5.0**, ADR-001…**012**). **Lock file su `channel:email:process` (anti-concorrenza cron, 2026-08-22, branch `feat/email-templates-v2`, working tree NON committato)**: nuovo modulo puro `src/cli/email-process-lock.ts` — lock derivato da `DB_PATH` (`<dir>/<nome>.lock`, UAT e produzione isolati), creazione esclusiva atomica `fs.writeFileSync('wx')`, contenuto = PID proprietario, staleness via `process.kill(pid,0)` (ESRCH → steal, EPERM → vivo) + fallback mtime `STALE_MS` 10 min, touch periodico `TOUCH_INTERVAL_MS` 30 s (`setInterval`+`unref`) — cablato nel handler `channel:email:process` (skip con log pino info inglese + messaggio CLI italiano + `--json {skipped:true,lockPath,holderPid}`, exit 0; rilascio nel `finally`); 10 test unit su directory temporanee; smoke test CLI end-to-end (skip JSON/testo con exit 0, steal su PID morto). **505 test verdi, typecheck/lint puliti.**

- **Phase:** POC under implementation — design aligned (**PRD v0.6.1, HLD v0.5.0, LLD v0.5.0**, ADR-001…**010**). **Iscrizione a livello di piattaforma (ADR-009) implemented 2026-08-20**: modello a due livelli con storage separato `PLATFORM_DB_PATH` (piattaforma solo letta dai flussi di torneo, nessuna scrittura cross-DB), `PlatformRegistry` con `registerID` stabile e soft-delete a due passi (`active`/`pending_unsubscribe`/`unsubscribed`), classificazione dell'intento via LLM in una chiamata (`LLMIntentClassifier`, comando `llm:classify`), auto-join al TT1 (RF-P5, profilo+pick atomici, risposta `pick_confirmed`), log silenzioso anti-spam (RF-P4), matrice notifiche con filtro account `active` su OGNI email (broadcast `tournament_open`, riepilogo `round_closed_survived` ai soli sopravvissuti alla transizione `closed→scored` con guardia `summary_sent`), comandi `platform:*`, scheduler senza azioni finestra iscrizione, simulazione con account piattaforma + auto-join e guardie su `PLATFORM_DB_PATH` di produzione/DB piattaforma pulito. **Piano test-e-fix dei findings della revisione tecnica 2026-08-21 eseguito integralmente (B1–B8, ADR-010)**: barriera unsubscribe intento-agnostica (completamento = `pending_unsubscribe` + body di conferma, indipendente dall'intento LLM), `summary_sent` atomico con la transizione a `scored` + invio best-effort, filtro notifiche fail-closed, guardia `simulate:*` ancorata a `PLATFORM_DB_PATH_DEFAULT`, silenzio `other` per account non `active`, `EmailType` `platform_already_registered`, auto-join su `player` legacy con backfill `register_id`, dead-write `registration_open` rimosso, commenti stale corretti, contract test `llm:classify` (D7 chiuso). **431 test verdi, typecheck/lint puliti.**
- **Work plan:** `tasks/plan.md` (implementation roadmap with decisions confirmed on 2026-08-13) + `tasks/aggancio-torneo-asincrono/plan-aggancio-torneo-asincrono.md` (hook-up requirements, ADR-008). Task 0 (documentation alignment and cleanup) and Task 1.1 (project bootstrap) completed. Phase 1 "System skeleton" completed: Task 1.2 (`src/config.ts`, `src/logger.ts`) and Task 1.3 (`src/db/connection.ts`, `src/db/schema.ts`, `db:migrate`) completed; Checkpoint 1 green. **Asynchronous-tournament plan executed (2026-08-14):** ADR-008 recorded; PRD/HLD/LLD aligned (RF-20…31, CL11–18, registration window anchored to the TT1, auto-registration, anti-fraud guard RF-31, forced/safety closures, eligibility seam); **additive migration `tournament_state.start_round`** implemented and tested on a legacy DB (18 green tests, clean typecheck/lint); requirements merged into Tasks 3.2/3.5/4.1/4.2/5.2/6.1/6.2/7.1/7.2 of `tasks/plan.md` (checkpoints updated). **Phase 2 "Season data" completed (2026-08-14):** Task 2.1 `FootballDataClient` (injectable fetch, throttling on `X-RequestsAvailable`/`X-RequestCounter-Reset` in seconds, retry only 429/5xx/network, timeout, `FootballDataError`); Task 2.2 `DbSeasonDataProvider` + `Match` type/`SeasonDataProvider` interface shared in `src/data/provider.ts` (`getFirstMatchDateTime` semantics fixed for RF-31; `getResults` removed from the interface as redundant); Task 2.3 commands `data:import`/`data:refresh`/`data:calendar`/`data:results` (idempotent, transactional upsert on PK `(round, home_team, away_team)`, canonical `match_date` format ISO-8601 UTC); Task 2.4 `src/llm/team-aliases.md` (prompt resource, 20 clubs, API `name` values); Task 2.5 fixture 4 teams/6 rounds with staged loader and mutation helper (CL7/CL1/CL8/frozen→evaluated). **Checkpoint 2 green** (70 tests, clean typecheck/lint: mock import → DB → provider working; aliases and fixtures ready). **Spec corrections applied (table §8 of the Phase 2 briefing):** `X-Requests-Available` → `X-RequestsAvailable`; `X-RequestCounter-Reset` in seconds (×1000 ms); full status mapping with `AWARDED`; `matchday → round`; canonical `match_date` format — applied in `tasks/plan.md`, ADR-007, LLD §6.1 and §7.2.
- **Architecture review:** no remaining blocking changes. The POC-relevant changes from `docs/reviews/2026-08-11/architecture-review-2026-08-11.md` have been absorbed into the design documents and the plan.
- **Phase 3 "Game Engine" completed (2026-08-14, Tasks 3.1–3.5 + 4.1 + 4.2):**
  - Task 3.1 Rules Engine (`src/game/rules.ts` + CLI `rules:*`): data-driven parameters (RF-19), half-season boundary `floor(N/2)+1` (LLD §3.1 — note: the briefing §2.2 phrased it as `ceil`; for even N they coincide, here LLD applies), burned/available teams per profile+half (frozen count as burned, CRITICAL-01), shared `pickOutcomeFor`.
  - Task 3.2 Pick Processor (`src/game/errors.ts`, `src/game/pick-processor.ts` + CLI `pick:*`): §1-F cascade with dedicated reason (10 reasons), **anti-fraud guard RF-31** (acceptance = min(recorded deadline, actual kickoff; CL17/CL18), US10 override `--reason` only on time checks, atomic register on UNIQUE (CS2/CL6).
  - Task 3.3 Elimination Engine (`src/game/elimination.ts` + CLI `elimination:*`): `missing_pick`/`wrong_pick`, idempotent, `eliminated_at` from the injected clock (decision A).
  - Task 3.4 Winner Engine (`src/game/winner.ts` + CLI `winner:check`): 3 cases (PRD §4.6), decision 2 (same wave from shared `eliminated_at`), frozen at end of season stays in the game (CS6), window-agnostic (CL12/RF-26).
  - Task 3.5 Round Manager (`src/game/round-time.ts`, `turn.ts` — shared modules §8; `src/channel/adapter.ts`, `src/llm/generator.ts` — §1-D interfaces; `src/game/round-manager.ts` + CLI `round:*`): open (fixed deadline RF-14, pick email with only available teams, decision 12), close (consolidates; `--force --reason` RF-29), score (incremental ADR-003: pending→correct/wrong, postponed beyond tcClose→frozen CL1/CL8, CL7 within window, frozen→evaluated also on scored, LLD §1.4; round→scored when no pending RF-16; idempotent RF-17), status/deadline (RF-31).
  - Task 4.1 Tournament (`src/game/tournament.ts` + CLI `tournament:start/status/history/leaderboard/export`): `--start-round` (RF-20), RF-21 validations with atomic rejection, CL12 warning, `registration_open=1` at start (RF-22), RF-30 anomalies in status.
  - Task 4.2 Registration (`src/game/eligibility.ts` — ADR-008 seam no. 8; `src/game/registration.ts` + CLI `tournament:register:*`): `register:open` (notify only once, additive migration `registration_notified`), `register:close` (RF-28), `register --reason` (US10), **auto-registration RF-27** (profile+pick atomic, CL2/CL5/RF-24), RNF2 uniqueness.
  - **Checkpoint 3 green (168 tests, clean typecheck/lint):** CS2/CS4/CS5/CS6 covered by dedicated tests; RF-31 (CL17/CL18) and RF-27 (CL2/CL5/RF-24) tested; game logic entirely outside channel and LLM (interfaces mocked in tests, LLD §8).
- **Phase 5 + Phase 6 "LLM Adapter + Channel Adapter" completed (2026-08-14, Tasks 5.1/5.2/6.1/6.2 — Checkpoint 4):** decisioni D1–D9 confermate e applicate (subject opzionale `sendMessage(to, body, subject?)` + `subjectFor`; `PickExtraction` unica in `src/llm/parser.ts` con `{teams, aliases}` per chiamata; `LLMError` per trasporto/HTTP vs `null` per contenuto; segnaposto `{{TT_TC}}`/`{{TTTC}}` con sostituzione post-generazione, mai numeri nel prompt; nuovo `EmailType auto_registered`; classificazione deterministica del Message Router + `normalizeEmail`; flag `\Seen` solo a successo; "round corrente" = primo round_state open; fuso fisso Europe/Rome it-IT). Task 5.1 (`src/llm/openai-client.ts`, `errors.ts`, `parser.ts` + CLI `llm:parse`), Task 5.2 (`src/llm/templates.ts` — 12 template; `src/llm/generator.ts` — `subjectFor` + `OpenAIGenerator`; CLI `llm:generate`), Task 6.1 (`src/channel/email-adapter/` — imap-client con internaldate ADR-001, smtp-client, message-router, EmailAdapter con `markSeen`; dipendenze `imapflow`/`nodemailer`/`mailparser` installate; CLI `channel:email:fetch`/`send`), Task 6.2 (`src/channel/email-processor.ts` wiring e2e + CLI `channel:email:process`; `src/cli/email-wiring.ts` inietta le componenti reali nei comandi `round:*`/`tournament:*`). **Checkpoint 4 green (237 tests, clean typecheck/lint):** contract test LLM con fetch mockato (CS7), RF-25 (coppia iniettata), e2e simulato CS1 con adapter/parser fake (CL2/CL5/RF-24/RF-27/RF-31, stop batch su LLMError), correzioni spec della tabella §7 del briefing applicate ai documenti (LLD §1.2/1.3/2/5/6.2/6.3/6.4/7.8, plan.md, PRD §4.1/CL2).
- **Phase 7 "Simulazione e scheduler" completed (2026-08-14, Tasks 7.1–7.3 — Checkpoint 5):** decisioni R1–R8 del briefing `tasks/briefing-fasi/briefing-fase-7.md` applicate. **Fix determinismo RNF1 (Decisione A):** `created_at` di player/profile/pick scritto esplicitamente dal clock iniettato (`insertPendingPick(..., createdAt)` + call-site `registerPick`/`autoRegisterFromPick`; `registerPlayer`/`autoRegisterFromPick` scrivono `ctx.now.toISOString()`) — due run con stessa seed producono export identici. Task 7.1 (`src/game/simulation.ts` + CLI `simulate:full [--start-round] [--seed]`/`simulate:round --round`): mulberry32 puro (seed default 42), clock derivato dai dati per fase (open/receivedAt a deadline − 1min, close a deadline + 1min, score a tcClose + 1min), guardia R3 (rifiuto se season_started=1 o round non-pending), contesto senza email (R1), report per round + vincitore; test integration `tests/integration/season-sim.test.ts` (11 test: CS3 tutti scored, RNF1 export identici a parità di seed/clock e diversi con seed diversa, guardie, CL13/CL12, simulateRound). Task 7.2 (`src/game/scheduler.ts` + CLI `scheduler:tick`/`scheduler:status`): `computeActions` pura (finestra iscrizione auto/safety RF-22/RF-30, open TT1 RF-23 e al termine del TC precedente, close a deadline, close_safety con causa `deadline_missing`, score e score_frozen con `SCHEDULER_AUTO_SCORE`), `schedulerTick(ctx, deps)` con `deps.refresh` iniettato (CLI: `importMatches`+`FootballDataClient`; errore → `refresh_failed` e prosegui, RNF9), check-then-act idempotente, `warn_not_calculable` + anomalia in `tournament:status`; `scheduler:status` = stato computato (R5, nessuna persistenza); CLI logga gli eventi con pino (warn per `*_safety`/`refresh_failed`/`warn_not_calculable`) e `scheduler:tick` esce senza effetti se `SCHEDULER_ENABLED=false`; test unit `tests/unit/scheduler.test.ts` (10 test con clock finto). Task 7.3: esempio crontab sotto §4.4 di `.env.example`. Correzioni documenti della tabella §6 applicate (LLD §1.4/§3/§5/§7.11/§7.12, plan.md Fase 7, PRD §9 nota RNF1). **Checkpoint 5 green (261 tests, clean typecheck/lint) + UAT CLI eseguita:** `data:import` reale (380 partite) → `simulate:full --seed 42` completa 38/38 TC scored (CS3) → doppio `tournament:export` identico salvo `exportedAt` (RNF1, diff 1 riga) → agganci `--start-round 20` (CL13, finestra TC 20..38) e `--start-round 38` (CL12, warning) senza errori → `simulate:round` su DB fresco OK e guardia R3 verificata da CLI → `scheduler:status` computato e `scheduler:tick` no-op se disabilitato; `tournament:start` su dati storici rifiuta correttamente (RF-21).
- **Next step:** review indipendente del Task 6 completata (Agent Manager "review task 6 guida uat" → `docs/reviews/2026-08-17/review-uat-task-6.md`): verdetto FAIL per 1 difetto bloccante (§5.3: confine di girone con 8 giornate indicato "dopo il round 7" invece di round 5 = `floor(8/2)+1`) — **corretto** (decouplato wrap vs reset del pool, testo allineato a `src/game/rules.ts:58-59` e all'output reale `confine girone 5`); 2 osservazioni minori non bloccanti (etichette durata ~4h30/~6h30 vs ≈4h45/≈6h15 — intentional, dalle figure prescritte; `tournament:status` "Iscrizioni: aperte" dopo `register:close`+`start` — comportamento engine fuori scope guida) lasciate invariate. Con Task 0–6 completati l'implementazione del piano UAT `tasks/uat/plan-uat-calendario-sintetico.md` è conclusa (Checkpoint D); resta la **revisione non-developer/commissioner** della guida (Checkpoint D) e la pianificazione dedicata dell'esecuzione UAT (piano §4, dipendente dall'esito di questo piano) — l'esecuzione NON fa parte del piano.

## Changelog

- `2026-08-30T22:50:00Z` — **Revisione indipendente della feature opt-in conclusa (PASS con raccomandazioni, `docs/reviews/2026-08-30/review-optin-tournament-participation.md`) + correzione dei punti prima del commit (working tree NON committato, stesso branch)**: **MEDIUM-1** commenti sorgente stale sul concetto rimosso "auto-join al primo pick"/RF-P5 aggiornati al modello opt-in (`schema.ts`, `eligibility.ts`, `pick-processor.ts`, `scheduler.ts`, `email-processor.ts`, `generator.ts`, `email-process.test.ts`, `season-sim.test.ts`); **MEDIUM-2** `platform:register` ora applica i due flag SOLO alla prima creazione (le riattivazioni preservano le preferenze; cambio via `platform:preferences`) — ADR-019 decisione 4, cli-reference e technical-administrator-manual aggiornati; **LOW-1** variante morta `tournament_started` rimossa dal tipo `DeclareResult` (allineati HLD diagramma join e system-components); **LOW-2** in `declareParticipation` il check `already_joined` è ora PRIMA della finestra TT1 (idempotenza D8 incondizionata: "sei già in gara" anche a TT1 chiuso) + nuovo test; **LOW-3** `.tmp/` aggiunto agli ignore di eslint (lint end-to-end verde). **743 test verdi (+1), typecheck e lint puliti.**

- `2026-08-30T18:45:00Z` — **Feature "partecipazione opt-in — registration ≠ join" implementata (piano `.kilo/plans/1788106316564-optin-tournament-participation.md`, TDD, branch `feat/win-only-mode`, working tree NON committato — nessun commit/push)** + **FASE D (documentazione) completata (stesso branch, solo docs)**. **ADR-019 registrata** (append-only): RIMOSSO l'auto-join al primo pick (emenda ADR-009 decisione 6 e RF-P5; nota anche su ADR-018 D6). **T1 schema+registry**: `platform_account.receive_tournament_start_notification`/`tournament_auto_join` (`INTEGER NOT NULL DEFAULT 1`, DDL + `applyPlatformAdditiveMigrations` idempotente); `PlatformAccount` con i due flag; `register` scrive i flag a 1 SOLO alla prima creazione (le riattivazioni non li toccano); nuovi `activeAccounts()` (ordinati per register_id) e `setPreferences(email, prefs)` (senza timestamp); `activeEmails()` invariato (usato da `tournament:status`/scheduler). **T2 CLI**: `platform:register --auto-join --receive-notifications` (bool, default true, applicati via `setPreferences`) + nuovo `platform:preferences --email [--auto-join on|off] [--receive-notifications on|off] [--json]` + `platform:list` con i due flag. **T3 motore**: `createProfileForAccount` (unica fonte profilo, riuso/backfill `register_id` legacy, `jollies_remaining` = `JOLLIES_PER_PLAYER`), `autoJoinProfilesAtStart` (snapshot a `tournament:start`, idempotente, no-op senza registry), `declareParticipation` (esito `DeclareResult`: ok/not_active/no_tournament/tournament_started/already_joined/late_requires_reason; finestra TT1 = round 1 pending/open; override con `--reason` fuori finestra); RIMOSSO `autoJoinFromPick`; `startTournament` con auto-join bulk DOPO il reset su torneo chiuso + campo `autoJoined` e broadcast `tournament_open` filtrato su `receiveTournamentStartNotification`; nuovo comando `tournament:join --email [--reason]` (contesto senza channel/generator, D12). **T4 intento join**: `MessageIntent` += `join`, prompt LLM dedicato ("partecipo" spostato da subscribe a join), formula `PARTECIPO` nel parser deterministico (ordine disiscrizione→iscrizione→partecipo→pick). **T5 wiring**: ramo `join` in `channel:email:process` (null/unsubscribed → log silenzioso RF-P4; pending → reactivate; poi `declareParticipation` → `tournament_join_confirmed`/`tournament_already_joined`/`tournament_join_rejected` con reason `no_tournament`/`tournament_started`/`not_in_tournament`); pick da attivo senza profilo NON fa più auto-join (TT1 → `not_in_tournament` "per partecipare invia PARTECIPO", TT2 → `rejected_tt2`); `ProcessedAction` += `join_confirmed`/`join_rejected`/`already_joined`; `clarification` insegna 4 opzioni. **T6 round:open**: rimosso il blocco `registeredNotified` (eccezione TT1, RF-P6) e il campo da `RoundOpenResult` + testo CLI. **T7 simulazione**: account sim registrati PRIMA di `startTournament`; `simulateRound` invoca esplicitamente `autoJoinProfilesAtStart`; rimosso il ramo TT1 speciale (seed RNG cambiato, accettato RNF1). **T8 email**: 3 nuovi `EmailType` (`tournament_join_confirmed` soggetto "Partecipazione Confermata" / `tournament_already_joined` "Già in Gara" / `tournament_join_rejected` "Partecipazione Non Confermata" con reason) in `generator.ts`/`templates.ts`/`email-renderer.ts`; CTA `tournament_open` "Per partecipare al torneo, rispondi con \"PARTECIPO\"."; `tournament_join_confirmed` in `PICK_EMAIL_TYPES` (deadline in coda solo se round aperto). **T9 test**: registry (flag ON, setPreferences, migrazione), registration (createProfileForAccount/autoJoinProfilesAtStart/declareParticipation, rimossi test autoJoinFromPick), tournament-closure, tournament, round-notifications, deterministic-parser/intent-classifier (PARTECIPO→join), email-process (ramo join + anti-spam), season-sim (seed), generator/deterministic-generator. **Documentazione**: PRD v0.6.2 (RF-P5/RF-P6/RF-06/RF-10/glossario/US1/US7/US10/CL2/CL5/CL10/CL16/§12/§14), HLD v0.6.0, LLD v0.6.2, cli-reference (`platform:preferences`, `tournament:join`, opzioni `platform:register`, `round:open` senza `registeredNotified`, `tournament:start` con `autoJoined`, `channel:email:process` join), technical-administrator-manual (matrice notifiche, flag via CLI, modello registration/join, §6.2/6.3/6.4, glossario), system-components, guida test-mode (§1.2 parser PARTECIPO, §3.1 opt-in, matrice notifiche, nuovo scenario §5.6, §6.1, glossario), system-prompt-agente-test-mode (residui auto-join). **Suite di test verde (TDD, conteggio in verifica), typecheck/lint da eseguire.**

- `2026-08-30T12:45:00Z` — **Revisione indipendente feature JOLLY conclusa con PASS + raccomandazioni** (`docs/reviews/2026-08-30/review-jolly-feature.md`): 710 test verdi, typecheck/lint puliti, allineamento win_only + autopick verificato punto per punto (9/9), nessun finding bloccante/high/medium. **Corretti i 3 LOW prima del commit**: commento cascata `pick-processor.ts` aggiornato con i passi jolly 6bis (`jolly_not_allowed`/`no_jollies_left` DOPO `pick_already_exists` e PRIMA dei check temporali); commento seed `simulation.ts` precisato (dipende da `WIN_ONLY` e dall'extra draw jolly; con jolly off sequenza classica invariata); ADR-018 chiarita sulla numerazione ("secondo incremento" = designazione del piano, atterrato dopo l'auto-pick che ha occupato ADR-017). **Feature JOLLY committata** (commit unico, `feat(game): jolly...`).

- `2026-08-30T12:15:00Z` — **Feature JOLLY implementata (piano `.kilo/plans/1788027046413-jolly-feature.md`, TDD, branch `feat/win-only-mode`, working tree NON committato — nessun commit/push)**. **T1 config**: `JOLLIES_PER_PLAYER` in `src/config.ts` §4.1 (`z.coerce.number().int().nonnegative().default(1)`, commento scopo/valori/effetto: `0`=off, `≥1`=on) + `.env.example`/`.env.uat.example`/`.env.uat-replay.example` + `config.test.ts` (default `1`, conversione, `0` accettato, negativo/non-intero rifiutati). **T2 `GameMode` estensibile**: nuovo `src/game/mode.ts` con `GameMode { winOnly, jollyEnabled }` + factory pura `modeFor(winOnly, jolliesPerPlayer)` (`jollyEnabled = winOnly && jollies >= 1`); refactor meccanico del parametro `winOnly: boolean` → `mode: GameMode` in `renderEmailV2`, `narrativeFor`, `templateFor`, `deterministicNarrative`, `OpenAIGenerator`, `DeterministicGenerator` e call-site (wiring/CLI con `modeFor(config.WIN_ONLY, config.JOLLIES_PER_PLAYER)`) — nessun cambio di comportamento. **T3 schema additivo**: `tournament_state.jollies_per_player INTEGER NOT NULL DEFAULT 1` + `profile.jollies_remaining INTEGER NOT NULL DEFAULT 1` + `pick.jolly_used INTEGER NOT NULL DEFAULT 0` (DDL + `applyAdditiveMigrations` idempotenti); `getTournamentState`/`startTournament` UPSERT/`tournamentExport` includono `jollies_per_player`. **T4 guardia**: `assertModeConsistent` confronta anche `jollies_per_player` vs `JOLLIES_PER_PLAYER` (stessa funzione, fatal + throw con messaggio che nomina la variabile). **T5 motore**: `PICK_REJECT_REASONS` += `jolly_not_allowed`/`no_jollies_left`; `PickInput.jolly`; profile query con `jollies_remaining`; gate jolly DOPO `pick_already_exists` e PRIMA dei check temporali; `insertPendingPick(..., jollyUsed)`; `registerPick` con `db.transaction` (insert + decremento atomico); `autoJoinFromPick` con INSERT profilo `jollies_remaining` e decremento nel BEGIN/COMMIT. **T6 parser/classificatore**: `PickExtraction.jolly`; `PickParseOptions.jollyEnabled`; `DeterministicIntentClassifier` con keyword `\bjoll[yi]\b` (word boundary, case/accenti-insensibile, rimossa prima della risoluzione) — jolly off = rumore; `classificationSchema.pick.jolly`; prompt LLM jolly + `parseClassification` che propaga `jolly` (gated su `jollyEnabled`). **T7 scoring**: `PickRow.jolly_used`; `evaluatePick` con `savedByJolly` (wrong + jolly + draw → correct, nessuna eliminazione); `ScoredPick`/payload email con `jollyUsed`/`savedByJolly`; `getRoundPlayers` con `jolly: true`; `getActiveProfiles` con `jollies_remaining` (istruzioni/riepilogo). **T8 wiring**: `jollyEnabled` nel classify di `channel:email:process`; `jolly` a `registerPick`/`autoJoinFromPick`; conferme con `jollyUsed` + `jolliesRemaining`; motivi jolly in italiano (`non hai più jolly disponibili`/`il jolly non è ammesso in questa modalità`). **T9 testi email**: chiave `PICK REGISTRATO CON JOLLY → {TEAM}`; righe 🎯 (istruzioni «SQUADRA Jolly», salvato `🎯 Il tuo jolly ti ha salvato: {SQUADRA} ha pareggiato.`, vittoria `🎯 Jolly usato`, sconfitta `🎯 Il jolly non salva dalla sconfitta.`, `🎯 Jolly rimasti: N`); marcatore ` · 🎯 Jolly` in `playerResultRow`; `narrativeFor` con `savedByJolly` (mai "hai indovinato"); `JOLLY_TEMPLATE_OVERRIDES`; `serializeEmailContext` con righe jolly; overlay selezionato da `templateFor`. **T10 CLI**: `pick:* --jolly`; `llm:parse`/`llm:classify` con `jollyEnabled` e output jolly; `llm:generate --jolly-used`/`--jollies-remaining`. **T11 simulazione**: seed jolly con extra `rng()` (probabilità 0.25) SOLO con `jollyEnabled` e contatore > 0 (sequenza classica invariata quando off); `simulateRound` UPSERT con `jollies_per_player`. **T12 test**: 63 nuovi (config 4, mode 4, pick-processor 7, registration 2, deterministic-parser 7, intent-classifier 6, email-renderer 13, deterministic-generator 3, generator 6, email-process 4, round-flow 4, season-sim 2, tournament 2 — conteggio indicativo) — **710 test verdi, typecheck/lint puliti**. **T13 documentazione**: **ADR-018** registrata (append-only, ultima voce = ADR-017); LLD v0.6.1 (env var §4.1 + nota modello dati §3), technical-administrator-manual (regole, matrice notifiche, env var table, guardia §6.8), cli-reference (`pick:validate`/`pick:register` `--jolly`, `llm:*` jolly), guida test-mode (nota `<TEAM> Jolly` + attivazione UAT), system-components (GameMode/modeFor, parametro, tabelle); current-status aggiornato.

- `2026-08-30T09:36:00Z` — **Discrepanze documentali risolte (seguito della FASE T11, solo docs, branch `feat/win-only-mode`, working tree NON committato — nessun commit/push)**. **`docs/cli-reference.md` `data:seed-synthetic`**: "Serie B" → rosa sintetica **Serie A 2026/27** (testo allineato al sorgente `SYNTHETIC_TEAMS`, in concorrenza con l'aggiornamento rosa dell'altro agente, che ha atterrato la stagione esplicita sugli stessi righi — mantenuta "2026/27" su conferma utente), `--teams` "2 to 8" → "2 to 20", mixed-calendar "real Serie A + synthetic Serie B" → "real season + synthetic Serie A 2026/27". **`docs/POC/POC_LLD.md`**: **§5 albero progetto re-syncato alla struttura reale di `src/`** (0 file mancanti / 0 fantasma: aggiunti `clock.ts`, `logger.ts`, `game/context.ts`/`errors.ts`/`mode.ts`/`round-time.ts`/`turn.ts`, `data/importer.ts`/`synthetic-season.ts`, `llm/email-renderer.ts`/`deterministic-generator.ts`/`deterministic-parser.ts`/`errors.ts`, `channel/email-adapter/reply-cleaner.ts`, `cli/archive-wiring.ts`/`email-process-lock.ts`/`output.ts`, `cli/commands/db.ts`, `team-aliases-synthetic.md`; rimossi i fantasmi `db/seed.ts` e `print.ts`); **§7.1** rimossa la riga fantasma `npm run db:seed` (il file non esiste; esiste solo `db:migrate`); **§7.2** aggiunto `data:seed-synthetic`. Nessuna modifica a `src/`, `tests/` o `.env*` (quelle presenti nel working tree sono dei piani win_only/AUTOPICK e dell'aggiornamento rosa 2026/27).

- `2026-08-30T09:31:00Z` — **Rosa del calendario sintetico allineata alla Serie A 2026/27** (richiesta utente; branch `feat/win-only-mode`, working tree NON committato). `SYNTHETIC_TEAMS` (`src/data/synthetic-season.ts`) = 20 nomi canonici reali dell'API football-data.org stagione 2026/27 (scarica live con il token di `.env`, `FOOTBALL_DATA_SEASON=2026`): AC Milan, AC Monza, ACF Fiorentina, AS Roma, Atalanta BC, Bologna FC 1909, Cagliari Calcio, Como 1907, FC Internazionale Milano, Frosinone Calcio, Genoa CFC, Juventus FC, Parma Calcio 1913, SS Lazio, SSC Napoli, Torino FC, US Lecce, US Sassuolo Calcio, Udinese Calcio, Venezia FC (rimossi AC Pisa 1909, Hellas Verona FC, US Cremonese). `SYNTHETIC_TEAM_SHORT_NAMES` allineata agli shortName API (es. `Como 1907`/`Venezia FC` non abbreviati). Risorsa `src/llm/team-aliases-synthetic.md` riscritta (20 canonici + alias editoriali, aggiunti monza/biancorossi, frosinone/ciociari/canarini, venezia/arancioneverdi/lagunari). Prompt test mode in `parser.ts`/`intent-classifier.ts`: "rosa di Serie A 2026/27, stagione fittizia". `scripts/seed-seriea-synthetic.mjs`: rimossa la lista hardcoded, importa `SYNTHETIC_TEAMS`. **Test (TDD)**: `synthetic-season.test.ts` (rosa 20 2026/27), `parser.test.ts` (marcatore promossa `Frosinone Calcio` vs assente `Empoli FC`), `intent-classifier.test.ts`/`parser.test.ts` (stringa prompt 2026/27), `team-aliases-synthetic.test.ts` (commenti). **Documentazione**: `docs/uat/guida-test-mode.md` (§1.2 rosa+alias 2026/27, riferimenti rosa sintetica, esempio SQL §5.5 con squadre attuali), `docs/uat/system-prompt-agente-test-mode.md` (§3.3), `docs/technical-administrator-manual.md` (§3.5/§5 "Serie B" → Serie A 2026/27, `--teams` 2–20), `docs/cli-reference.md` (`data:seed-synthetic`, llm:parse), `.env.example`/`.env.uat.example`/`.env.uat-replay.example` (commenti). Nota: riferimenti "Serie B"/vecchia rosa in `tasks/` e `docs/reviews/` restano storici (congelati). **647 test verdi, typecheck/lint puliti.**

- `2026-08-30T09:15:00Z` — **FASE T11 (documentazione) del piano `.kilo/plans/1788074961317-autopick-on-missing.md` completata — feature AUTOPICK** (branch `feat/win-only-mode`, working tree NON committato — nessun commit/push). **ADR-017 registrata** (append-only, `docs/decisions/architecture-decisions.md`: auto-pick al mancato invio `AUTOPICK_ON_MISSING`; D1-D12). **HLD/LLD allineati**: regola "auto-assign ⇔ `round_state.deadline !== null`" in `closeRound`; nuova tabella `team (name TEXT PRIMARY KEY, short_name TEXT NOT NULL)` popolata da `data:import`/`data:refresh`/`data:seed-synthetic` (`upsertTeams`, derivazione `deriveTeams`, campi transitori `Match.homeTeamShort`/`awayTeamShort`, mappatura `SYNTHETIC_TEAM_SHORT_NAMES`); colonne additive `tournament_state.autopick_on_missing` e `pick.auto_pick` (entrambe default 0, migrazioni idempotenti); env var `AUTOPICK_ON_MISSING` (bool, default `false`, attiva solo con `WIN_ONLY=true`, gating silenzioso); `assertModeConsistent` estesa ad `autopick_on_missing` (stessa guardia fatal, messaggio verbatim che nomina `AUTOPICK_ON_MISSING`); `getFirstAvailableTeamByShortName` nel Rules Engine; nuovo `EmailType pick_auto_assigned` (soggetto `Pick Auto Assegnato`, `keyMessage` `PICK AUTO ASSEGNATO → {TEAM}`, NON in `PICK_EMAIL_TYPES` — niente deadline); marcatore ` · 🤖 Auto-assegnato` in `playerResultRow` (campo `EmailPlayerResult.autoPick`); comando `rules:teams` (ordinato per `short_name`, output `<shortName> (<name>)`, `--json` → `[{name, shortName}]`, messaggio tabella vuota verbatim). **Documenti aggiornati**: `docs/POC/POC_HLD.md`, `docs/POC/POC_LLD.md` (v0.6.0 + changelog), `docs/technical-administrator-manual.md` (regole, matrice notifiche `pick_auto_assigned`, env var table §7.2, guardia fatal §6.8 estesa, ciclo round §6.4), `docs/cli-reference.md` (`rules:teams` + TOC), `docs/uat/guida-test-mode.md` (attivazione `AUTOPICK_ON_MISSING` in UAT, verifica ordinamento via `rules:teams`, nota §1.2, matrice notifiche §3.1, scope §6.1), `docs/system-components.md` (tabella `team`, `getTeamsOrderedByShortName`, `upsertTeams`, `rules:teams`, guardia estesa). Implementazione (T1-T10) e test NON toccati (agente solo documentazione).

- `2026-08-30T09:03:00Z` — **Feature AUTOPICK implementata (piano `.kilo/plans/1788074961317-autopick-on-missing.md`, TDD, branch `feat/win-only-mode`, working tree NON committato — nessun commit/push)**. **T1 config**: `AUTOPICK_ON_MISSING: boolParam('false')` in `src/config.ts` §4.1 (accanto a `WIN_ONLY`, commento scopo/valori/effetto) + `.env.example`/`.env.uat.example`/`.env.uat-replay.example` + `config.test.ts` (default `false`, `true` accettato). **T2 shortName + tabella `team`**: `Match.homeTeamShort`/`awayTeamShort` + tipo `Team`; `football-data-client` estrae e valida `shortName`; nuova tabella `team (name TEXT PRIMARY KEY, short_name TEXT NOT NULL)`; `upsertTeams` in importer (import/refresh/seed la popolano); `SYNTHETIC_TEAM_SHORT_NAMES` nel generatore sintetico; `getTeamsOrderedByShortName()` in db-provider. **T3 CLI `rules:teams`**: elenca la tabella `team` ordinata per `short_name`, testo `<shortName> (<name>)` per riga, `--json` → `[{name, shortName}]`, messaggio tabella vuota verbatim, zero chiamate API. **T4 schema additivo**: `tournament_state.autopick_on_missing` + `pick.auto_pick` (entrambi `INTEGER NOT NULL DEFAULT 0`, migrazioni idempotenti via `PRAGMA table_info`). **T5 persistenza**: `autopick_on_missing` scritta a `tournament:start` (stessa UPSERT di `win_only`), inclusa in `getTournamentState`/`tournamentExport` (RNF1; `pick.auto_pick` entra via `SELECT *`). **T6 guardia**: `assertModeConsistent` estesa al confronto `autopick_on_missing` vs `AUTOPICK_ON_MISSING` (stessa funzione, messaggio fatal verbatim che nomina la variabile). **T7 motore — auto-assign in `closeRound`**: solo con `autopickEnabled && rs.deadline !== null` (D3); `getFirstAvailableTeamByShortName` nel Rules Engine (riusa `getBurnedTeams` + filtro in-giornata + ordinamento `short_name`, dati letti UNA volta dal chiamante); `insertPendingPick(..., autoPick=1)` con `outcome='win'`; notifica `pick_auto_assigned` best-effort; fallback `missing_pick` + warn se nessuna squadra disponibile (D6); `RoundCloseResult.autoAssigned`; `getRoundPlayers` popola `autoPick`. **T8 email `pick_auto_assigned`**: 18° `EmailType` (soggetto `Pick Auto Assegnato`, keyMessage `PICK AUTO ASSEGNATO → {TEAM}`, NON in `PICK_EMAIL_TYPES` — niente deadline), template LLM + `DETERMINISTIC_NARRATIVES`. **T9 marcatore**: ` · 🤖 Auto-assegnato` in `playerResultRow` (campo `EmailPlayerResult.autoPick`) → presente in `round_closed_survived` e `tournament_closed` (speculare al futuro jolly). **T10 test**: nuovo `tests/integration/autopick.test.ts` + aggiornamenti a config/db-provider/importer/football-data-client/mode/tournament/round-manager/round-flow/email-renderer/deterministic-generator/generator; `npm test` verde (647 test a valle delle successive modifiche rosa 2026/27), `npm run lint` e `npm run typecheck` puliti.

- `2026-08-28T19:14:00Z` — **Mail di esito round in `win_only`: riga pick dopo l'esito (richiesta PO)**. In `src/llm/email-renderer.ts` `resultLine(ctx, winOnly)` mostra, per `round_result_correct`/`round_result_wrong` in win_only, `⚽ il tuo Pick: {TEAM} ⚽` (squadra in MAIUSCOLO) subito dopo ✅/❌ e prima della narrativa; `pick_missing_elimination` non riceve la riga (nessun pick); modalità classica invariata. 3 test aggiunti in `tests/unit/llm/email-renderer.test.ts` (622 test verdi, typecheck/lint puliti).

- `2026-08-28T18:46:00Z` — **`win_only` diventa la modalità di DEFAULT (`WIN_ONLY` default `true`)**. `src/config.ts` §4.1: `WIN_ONLY: boolParam('true')` (era `'false'`); `WIN_ONLY=true` esplicitata in TUTTI gli env file (`.env.example`, `.env.uat.example`, `.env.uat-replay.example`, e i reali `.env`, `.env.uat`, `.env.uat-replay`, `.env.bak`, `.env.uat-repro.local`) con commenti aggiornati ("true (DEFAULT) = il giocatore sceglie solo la squadra che vincerà"); test allineati: `config.test.ts` default `true`, helper di test con `WIN_ONLY: 'false'` esplicita nei casi classici (email-process, season-sim, pick-processor); documentazione allineata (technical-administrator-manual, LLD §4, ADR-016, guida test-mode).

- `2026-08-28T17:15:00Z` — **Modalità `win_only` implementata (piano `.kilo/plans/1787928380301-win-only-mode.md`, TDD, branch `feat/win-only-mode`, working tree NON committato — nessun commit/push)**. Modifica di gioco opzionale `WIN_ONLY` (bool, default `false`): il giocatore sceglie SOLO la squadra che vincerà (`outcome='win'`), pareggio o sconfitta = pick sbagliato → eliminazione. **T1 config**: `WIN_ONLY` in `src/config.ts` §4.1 (boolParam) + `.env.example`/`.env.uat.example`/`.env.uat-replay.example` + test. **T2 persistenza**: colonna additiva `tournament_state.win_only INTEGER NOT NULL DEFAULT 0` (DDL + `applyAdditiveMigrations`), scritta a `tournament:start` (UPSERT unica, riscritta su riavvio a torneo chiuso), inclusa in `getTournamentState`/`tournamentExport`. **T3 guardia fatal generica** `assertModeConsistent(ctx)` in nuovo `src/game/mode.ts` (nome generico, pronto per Jolly): mismatch a torneo APERTO → `logger.fatal` + throw che abbatte il processo (stato DB invariato, precede le scritture); hook in `schedulerTick`, `processEmailBatch`, `openRound`/`closeRound`/`scoreRound`, `validatePick` (difesa in profondità); `logger` iniettato nel contesto di `channel:email:process`. **T4 motore**: cascata `invalid_outcome` ristretta a `['win']` in win_only. **T5 parser/classificatore**: `PickParseOptions.winOnly`; parser deterministico accetta squadra NUDA (`{team,'win'}`, decisione P1) e rifiuta draw/lose espliciti (→ chiarimento); classificatore LLM con prompt WIN_ONLY e `parseClassification` mode-aware (draw/lose → pick null, null/win → 'win'). **T6 wiring**: `winOnly` nel classify + motivi di rifiuto/chiarimento win_only ("solo il nome della squadra che vincerà"). **T7 testi email**: overlay `WIN_ONLY_NARRATIVE_OVERRIDES`/`WIN_ONLY_TEMPLATE_OVERRIDES` + helper `narrativeFor`/`templateFor` in `templates.ts`; `renderEmailV2(…, winOnly)` (CTA pick_instructions, `pickConfirmedKey` senza esito, `playerResultRow` senza "· esito"); `OpenAIGenerator`/`DeterministicGenerator` con `winOnly`; email-wiring e `llm:generate` allineati. **T8 CLI**: `pick:validate`/`pick:register --outcome` opzionale senza default (omesso → `invalid_outcome`, decisione P2); `llm:parse`/`llm:classify`/`llm:generate` iniettano `winOnly`. **T9 simulazione**: seed outcome solo `win` in win_only + `simulateRound` scrive `win_only` (export coerente). **T10 test**: `config.test.ts`, `pick-processor.test.ts`, `mode.test.ts` (10), `tournament.test.ts`, `deterministic-parser.test.ts`, `intent-classifier.test.ts`, `email-renderer.test.ts`, `deterministic-generator.test.ts`, `generator.test.ts`, `email-process.test.ts` (win_only), `season-sim.test.ts` (smoke win_only) — **619 test verdi**. **T11 documentazione**: ADR-016 registrata (append-only) + indice; manuale (§regole win_only, §7.2 `WIN_ONLY`, §6.8 guardia fatal, glossario); cli-reference (`--outcome` opzionale + note win_only su pick:*/llm:*); guida test-mode (§1.2 nota `<TEAM>` win_only + attivazione, glossario); POC_LLD §4 (`WIN_ONLY`) + nota `tournament_state.win_only`; system-components (`mode.ts`, `WIN_ONLY`, nota colonna); current-status. **Validazione finale: `npm test` (619 verdi), `npm run lint`, `npm run typecheck` in corso.** Jolly = piano dedicato successivo (forward-compatible: guardia generica, distinzione draw/lose preservata nello scoring, `PickExtraction` estensibile).

- `2026-08-26T18:52:00Z` — **Email V3+V4 merged su `main` (PR #3, squash `67588f7`)** — chiusa la fase branch `feat/email-v3` (eliminato localmente e sul remoto; resta solo `main`). **Setup VPS per il deploy (2026-08-26, sessione "deploy VPS")**: chiave SSH dedicata `~/.ssh/id_ed25519_vps` (ed25519, senza passphrase, KDF `-a 100`) + blocco `Host survivor-vps` in `~/.ssh/config` (`root@147.93.123.121`, `IdentitiesOnly yes`, chiave GitHub non toccata); fingerprint host `SHA256:DxYMAws4R+KXo70Jv1qUvUPFj2/jO5llAyEfDWVV3TE` confermata dall'utente e registrata in `known_hosts` (entry deduplicate); `ssh-copy-id` eseguito dall'utente (password di root, mai in chat) → accesso senza password verificato (hostname `srv1822163`); nuovo file **`vps-access`** (info di connessione, fingerprint, chiavi) aggiunto a `.gitignore` (credenziali, mai committare). **Verifica prontezza host (sola lettura) completata**: Ubuntu 24.04.4 LTS (x86_64), 2 core / 7.8GiB RAM (6.2 disponibili), 96G disco (89G liberi), cron attivo, fuso UTC, git 2.43.0, glibc 2.39 (prebuilt better-sqlite3 OK), python3 3.12.3, egress OK (imap.gmail.com:993, smtp.gmail.com:587, api.football-data.org:443, openrouter.ai:443). **Installazione Node.js**: Node **22.23.2 LTS** via NodeSource (`deb.nodesource.com/node_22.x`) + `build-essential` (make 4.3, g++ 13.3) — full-ICU confermato (locale it-IT ok). Note d'ambiente: sulla VPS è già presente Docker (repo `download.docker.com` configurato, da valutare per la strategia di deploy) e c'è un **reboot pendente** (kernel 6.8.0-134 → 6.8.0-138, il riavvio non è automatico). Gap noti per il deploy: comando `health:check` NON ancora implementato (requisito di produzione HLD §9 / HIGH-05 / R2) e percorso di esecuzione in produzione da definire (oggi CLI via tsx; valutare build `tsc` → `node dist/index.js`).

- `2026-08-25T16:45:00Z` — **Email V4 eseguita integralmente (piano `.kilo/plans/1787675020248-email-v4-chiusura-torneo.md`, TDD, branch `feat/email-v3`, working tree NON committato — nessun commit/push)**: sette cambi PO sulle email, tutti su canale email + Game Engine (nessuna modifica alle interfacce `ChannelAdapter`/`LLMGenerator`/`LLMIntentClassifier`, `tasks/llm/regole-email-design.md` NON toccato). **D1 — `pick_confirmed` senza frase di correzione**: `DETERMINISTIC_NARRATIVES.pick_confirmed = ''` + template LLM senza "può correggere la scelta" (verifica fattuale: il secondo pick è rifiutato da `pick_already_exists` + `UNIQUE(profile_id, round)`). **D2 — Header "Round del torneo N · Turno di Campionato M"**: nuove label dedicate `roundHeaderLabel`/`championshipHeaderLabel` in `src/game/turn.ts` (rimosso `championshipLabel` morto); `header()` del renderer le usa; le bruciate restano "(Round N)". **D3 — Elenco giocatori nel riepilogo**: nuovo campo `EmailContext.players?: EmailPlayerResult[]` (nome/squadra/esito/eliminato) + sezione renderer `👥 GIOCATORI DEL ROUND` (solo con `players`); popolamento in `scoreRound` con query per-round (partecipanti = `eliminated = 0 OR eliminated_at >= opened_at`, LEFT JOIN pick, computata UNA volta). **D4 — Co-vincitori**: `EmailContext.coWinners?: string[]` (gli ALTRI vincitori) + sezione `🤝 HAI CONDIVISO LA VITTORIA CON`; popolamento in `settleWinnerIfNeeded` da mappa `profileId → nome`. **D5 — Nuova mail `tournament_closed`**: 17° `EmailType` (soggetto `Chiusura Torneo` senza turno, `keyMessage` `🏆 TORNEO CONCLUSO!`, narrativa vuota), campo `EmailContext.tournamentHistory?: EmailTournamentRound[]` + sezione `📜 STORICO DEL TORNEO`; generazione con `buildTournamentHistory` (sola lettura, per round non `pending`) e invio UNA volta (guardia `guarded.changes === 1`), best-effort, a TUTTI i partecipanti (profili con almeno un pick, vincitori inclusi). **D6/D7 — Test strutturali**: deadline in coda (`.endsWith`) per i 4 tipi con pick; keyMessage MAIUSCOLO e primo dopo il saluto per i 17 tipi (finding documentato: `pick_rejected` con `reason` minuscolo verbatim). **D8 — ADR-015** registrata (append-only, con carve-out della convenzione 6 "mai elenchi nominativi" per i soli `round_closed_survived`/`tournament_closed`) + indice aggiornato (aggiunti anche ADR-013/014 mancanti) + LLD §6.2/§6.3/§6.4/§7.7/§7.8, cli-reference (`--type` + wording), technical-administrator-manual (matrice notifiche + chiusura), guida test-mode (glossario) allineati. **Verifica: 581 test verdi (48 file), typecheck e lint puliti.**

- `2026-08-24T18:24:00Z` — **Allineata la rosa del calendario sintetico alle 20 squadre reali di Serie A 2025/26 + fix mismatch alias/parser LLM (richiesta utente durante UAT)**. Durante la UAT con calendario sintetico Serie A e `AI_EMAIL_PARSER=true` il classificatore LLM registrava pick errati ("l'inter vince" → `US Cremonese`): la risorsa alias test `team-aliases-synthetic.md` conteneva gli 8 club di Serie B, incoerente con la rosa Serie A del calendario. **Modifiche**: `SYNTHETIC_TEAMS` (`src/data/synthetic-season.ts`) ora = 20 nomi canonici API Serie A 2025/26 (AC Milan, AC Pisa 1909, ACF Fiorentina, AS Roma, Atalanta BC, Bologna FC 1909, Cagliari Calcio, Como 1907, FC Internazionale Milano, Genoa CFC, Hellas Verona FC, Juventus FC, Parma Calcio 1913, SS Lazio, SSC Napoli, Torino FC, US Cremonese, US Lecce, US Sassuolo Calcio, Udinese Calcio); risorsa `src/llm/team-aliases-synthetic.md` riscritta (20 canonici + alias inter/l'inter/milan/napoli/juve/ecc.); prompt LLM di test mode in `parser.ts`/`intent-classifier.ts` ("campionato sintetico, rosa di Serie A 2025/26, stagione fittizia, NON la stagione reale"); descrizioni CLI `data:seed-synthetic` (2..20 squadre). **Test aggiornati (TDD)**: `synthetic-season.test.ts` (rosa 20, helper rosa fissa A–H per round-robin/wrap, default teams 20→10 partite/giornata), `team-aliases-synthetic.test.ts` (Checkpoint B: 20 club, Serie A), `seed-synthetic.test.ts` (`slice(0,8)`), `parser.test.ts`/`intent-classifier.test.ts` (stringhe prompt, marcatore AC Pisa 1909 vs Empoli FC), `deterministic-parser.test.ts` (commenti). **Documentazione**: `docs/uat/guida-test-mode.md` aggiornata (sezione squadre sintetiche 8→20 con alias, tabella parser, seed, glossario, WARN calendario misto). **Verifica**: 570/570 test verdi + smoke dal vivo `llm:classify --mode llm` ("l'inter vince" → FC Internazionale Milano, "vince il milan" → AC Milan). Nota: il calendario sintetico e la produzione ora condividono la stessa rosa di 20 club (differenza: stagione fittizia di test). Working tree con modifiche non committate (branch `feat/email-v3`).

- `2026-08-24T11:50:00Z` — **Email V3 Parte B eseguita integralmente (piano `.kilo/plans/1787519052097-email-v3-deterministic-parser.md`, TDD, branch `feat/email-v3`, 6 commit atomici, NON pushato/non PR)**: parser deterministico dell'INPUT email con interruttore `AI_EMAIL_PARSER`. **Task 1 — Config**: `AI_EMAIL_PARSER: z.boolean().default(false)` + `LLM_API_KEY` opzionale (default `''`) con superRefine che la rende obbligatoria quando `AI_EMAIL_GENERATOR` o `AI_EMAIL_PARSER` è true (run senza IA); `.env*.example` aggiornati. **Task 2 — Subject nel pipeline**: `IncomingMessage.subject` (adapter), popolato da `parsed.subject` (imap-client), router classifica con "corpo E subject vuoti" → unknown (prima solo corpo) e propaga `subject`, `PickParseOptions.subject`, `email-processor` lo passa al classificatore. **Task 3 — `DeterministicIntentClassifier`** (`src/llm/deterministic-parser.ts`): formule univoche `ISCRIZIONE [NOME]`→subscribe (nome a fine riga, trim, max 40 char), `DISISCRIZIONE`→unsubscribe, `<TEAM> <ESITO>`→pick (lista canonica + tabella alias parse, longest-match, normalizzazione maiuscole/accenti; sinonimi esito); altrimenti `other`. 27 test unit (formule, body reali UID 291/295, formule libere non riconosciute). **Task 4 — Wiring + `FallbackIntentClassifier`**: `email-wiring` seleziona per `AI_EMAIL_PARSER`; `EmailComponents.classifier` passa a `LLMIntentClassifier`; `FallbackIntentClassifier` su `LLMError` classifica col deterministico + warn pino `{reason}` (batch non si ferma). **Task 5 — CLI `--mode`**: `llm:classify`/`llm:parse` guadagnano `--mode llm|deterministic` (default = `AI_EMAIL_PARSER`); ramo deterministico senza client LLM. Smoke test CLI su DB sintetico senza `LLM_API_KEY`: formule riconosciute. **Task 6 — Documentazione**: ADR-014 registrata (append-only) + emendati ADR-009 decisione 5 (due implementazioni), HLD §2.2/§5.3/§10 (R1), LLD §6.2 + config + CLI --mode, PRD RF-P1 (formula `ISCRIZIONE [NOME]` sostituisce "voglio iscrivermi"), guida-test-mode + manuale tecnico. **570 test verdi (48 file), typecheck/lint puliti, working tree pulito.** **Deviazioni dichiarate:** (1) UID 293 "catanzaro" (squadra senza esito) → `other` (la grammatica stretta `<TEAM> <ESITO>` richiede entrambi), NON "US Catanzaro win" come nel criterio (che rifletteva il default leniente dell'LLM); UID 291/295 riprodotti correttamente. (2) Il vincolo salvato `registration_invitation_name_wording` è stato SOSTITUITO (come previsto dal piano, decisione 3): l'istruzione ovunque è `ISCRIZIONE [NOME]`. (3) Match della formula keyword = substring (il subject di una reply a una mail di sistema "… Iscrizione Confermata" contiene "iscrizione" → falso positivo possibile; mitigato dalla barriera unsubscribe e accettato come "più rigido"). **Next step:** TEST sul branch `feat/email-v3` (UAT TEST MODE con formule deterministiche, run senza IA completa), poi merge via PR.

- `2026-08-24T10:45:00Z` — **Email V3 Parte A eseguita integralmente (piano `.kilo/plans/1787519052097-email-v3-restyle.md`, TDD, branch `feat/email-v3`, 5 commit atomici, NON pushato/non PR)**: restyle dell'output email secondo le regole PO (plain-text, niente riquadri ASCII, emoji ammesse, messaggio chiave MAIUSCOLO) + generazione deterministica con interruttore `AI_EMAIL_GENERATOR`. **Task 1 — Subject V3** (`src/llm/generator.ts`): `subjectFor` produce `⚽🏆SURVIVOR LEAGUE🏆⚽ - Turno {TC} di Campionato - {etichetta}` (TC assente → senza prefisso); il soggetto porta il SOLO turno di campionato, la coppia "Round N · Turno di campionato M" resta nel corpo; etichette iper-condensate ("Iscrizione Confermata", "Round Aperto", "Pick Registrato", "Esito Round" neutro, …); `ctx.subject` continua a prevalere. Asserzioni soggetto aggiornate in generator/email-process/email-adapter/smtp-client/tournament-closure. **Task 2 — Renderer V3** (`src/llm/email-renderer.ts`): rimosso `makeBox` e i box ASCII (esito/deadline/bruciate) → sezioni a righe con titolo emoji + MAIUSCOLO; nuovo `keyMessage(ctx)` deterministico per tipo in MAIUSCOLO; esito solo `✅ SEI ANCORA IN GARA!`/`❌ SEI STATO ELIMINATO!`; deadline con data+countdown sulla stessa riga (`· Mancano circa …`); "SQUADRE GIÀ USATE" con formato "(Round N)"; titolo partite dinamico (RISULTATI con punteggi, PARTITE altrimenti); header omesso senza coppia TT/TC (il brand è nel separatore). Test riscritti sugli OUTPUT ESATTI dei 16 template. **Task 3 — Separatore di brand**: `SYSTEM_EMAIL_SEPARATOR` → `─── Survivor League ───` (costante unica invio/taglio, logica `startsWith` invariata). **Checkpoint 1 green (526 test, typecheck/lint puliti).** **Task 4 — Testi deterministici + `AI_EMAIL_GENERATOR`**: `DETERMINISTIC_NARRATIVES` (rinominata da `FALLBACK_NARRATIVES`, testi fissi corti, vuota per il riepilogo); nuovo `src/llm/deterministic-generator.ts` (`DeterministicGenerator` zero rete + `FallbackGenerator` su `LLMError`→deterministico con warn pino `{reason,type}`, batch non si ferma); `AI_EMAIL_GENERATOR: z.boolean().default(false)` in config + `.env*.example`; `email-wiring` seleziona il generatore per config; `llm:generate` guadagna `--mode llm|deterministic`. Test: `deterministic-generator.test.ts` + rename nei test esistenti. **Task 5 — Documentazione**: ADR-013 registrata (append-only) + emendati RF-25/glossario PRD, HLD (R1 mitigato dal generatore deterministico), LLD §6.3/§6.4/§7.8 + config, guida-test-mode (glossario), cli-reference (`llm:generate --mode`), manuale tecnico (config + glossario). **533 test verdi, typecheck/lint puliti, working tree pulito.** **Deviazioni dichiarate:** (1) data nelle email = output reale `Intl` it-IT ("sabato 19 settembre 2026 alle ore 17:30"), il piano mostra "alle 17:30" (approssimazione); (2) stato aggregato nelle mail di esito = "In gara: N" senza "Eliminati" quando `eliminatedWrong/Missing` assenti (il piano mostra "Eliminati: X" illustrativo — nessun campo totale nel modello dati, fuori scope Task 2); (3) istruzione di iscrizione nel template = "ISCRIZIONE [il tuo nome]" (dal piano), diverge dalla constraint salvata "voglio iscrivermi" — da riconciliare col PO; (4) i test del renderer sono 19 `it` (vs 20 precedenti) perché consolidati in 16 test a output esatto + 3 strutturali (copertura invariata/aumentata). **Fuori scope (Parte B separata):** parser deterministico (`.kilo/plans/1787519052097-email-v3-deterministic-parser.md`), da eseguire DOPO il merge di questa Parte A. **Next step:** merge di `feat/email-v3` su main (PR, non fatto — l'utente decide), poi Parte B su `feat/email-v3-parser`.

- `2026-08-23T17:20:00Z` — **UAT "risultati che arrivano dopo la creazione del calendario" eseguita dal vivo (commissioner, email reali) + nuovo scenario §5.5 nella guida test-mode**: sessione su DB freschi (torneo `uat-synthetic-pippo.db` pulito da zero, platform `uat-platform-pippo.db` intatto con i 3 account attivi Fulvio De Giovanni/Pippi/Fulvio), calendario compresso `--teams 4 --rounds 2 --spacing-min 10 --first-kickoff-offset-min 15 --seed 42`, `.env.uat` riportato a `SCHEDULER_ENABLED=false`. **Flusso dimostrato su ENTRAMBI i round**: azzeramento punteggi `match` via `UPDATE match SET home_score=NULL, away_score=NULL WHERE round=N` (simula partite SCHEDULED/IN_PLAY) → `round:score` con risultati assenti lascia il round `closed` con pick `pending` (output verbatim `Round N (TT n) → closed — valutati: 0, frozen: 0, eliminati: 0`, `scored_at=null`, `summary_sent=0`, nessun riepilogo — ramo incrementale ADR-003/CL7/RF-16) → iniezione risultati via `UPDATE ... SET home_score=…` (simula `data:refresh` FINISHED) → secondo `round:score` → `scored` con esiti `correct`/`wrong`, email `round_result_*` + riepilogo `round_closed_survived`. Round 1: 3/3 `correct`. Round 2: 1 `wrong` (Fulvio De Giovanni, Catanzaro perse 1-2) → **chiusura automatica torneo** (ADR-011): `winner_notified=1`/`finished_at` 17:11:52Z, vincitori **caso 3** `fulviodegiovanni@live.com`+`sara.zizzari@gmail.com`, export `data/exports/tournament-export-2026-08-23T17-11-52.481Z.json` (player 3, profile 3, pick 6, match 4, round_state 2, tournament_state 1, turns TT/TC), scheduler `Prossime azioni: nessuna`. Email reali mistral-nemo (`auto_joined` al TT1, `pick_registered` al TT2). **Documentazione**: nuova sezione `docs/uat/guida-test-mode.md` §5.5 "Scenario: risultati che arrivano DOPO la creazione del calendario" (query DB esatte per azzerare/iniettare i punteggi, output attesi verbatim, 8 passi, note operative su azzeramento prima di round:open e query solo su `match`); §6.1 esteso (nuovo bullet dimostrabile) e §6.2 (effetto refresh simulabile via §5.5). Non documentato (già testato in passato, richiesta utente): pick a round non ancora aperto → `round_not_open` (CL3). Solo documentazione, nessuna modifica al codice; working tree con modifiche non committate.

- `2026-08-23T13:30:00Z` — **Fix findings review uncommitted 2026-08-23 (piano 1787484219693) implementati + E2E TEST MODE su DB freschi completato**: **F2** `inGameCount` UNICO per run (helper `countInGame`, notify differite in closeRound/scoreRound: ogni email di eliminazione/esito porta lo stesso valore, coerente col riepilogo; 2 test nuovi RED→GREEN in round-notifications.test.ts); **F3** recupero EXPORT in `settleWinnerIfNeeded` (torneo chiuso con `export_path` NULL → al rientro di round:close/score ritenta SOLO l'archiviazione, mai rinotificare i vincitori, fallimento sempre loggato warn; `getTournamentState` esportato; 1 test nuovo RED→GREEN in tournament-closure.test.ts con seam fallente poi funzionante); **F4** CTA disiscrizione del renderer derivata da `UNSUBSCRIBE_CONFIRM_WORDS` (testo invariato); **F5** rimossa l'iniezione morta deadline/countdown dal riepilogo `round_closed_survived` (mai renderizzata: PICK_EMAIL_TYPES non la include; niente guardia LOW-3 duplicata); **F6** `getBurnedEmailTeams(..., startRound)` con `ttFor` puro — startRound letto UNA volta in openRound; **F7** scheduler su `getTournamentState`/`isTournamentClosed` condivisi (rimosse copia locale e predicati inline ×3); **F8** NON fatto (decisione utente: bottom-posting troppo oneroso, rischio accettato); **F1** eliminato `.check-inbox.mjs` (credenziali Gmail in chiaro — ruotare l'App Password, azione utente); **F9** eliminato `templates.old.ts` + commenti templates.ts/LLD aggiornati. **526 test verdi, typecheck/lint puliti.** **E2E in TEST MODE su DB freschi** (`/tmp/kilo/e2e-review.env`, seed 4 squadre 2 giornate offset 120'/90', 3 account piattaforma +e2e1/2/3 e 3 profili): tournament:start → round:open R1 → 2 pick (Brescia win, Cremonese win) + 1 mancante → round:close (eliminato missing, email con inGameCount=2) → score (scores via script: 2-0 e 0-2 → 1 correct + 1 wrong eliminato, email con inGameCount=1) → **CHIUSURA AUTOMATICA caso 1** (vincitore +e2e1, winner_notified=1, export `/tmp/kilo/e2e-exports/...json` verificato su disco) → scheduler:tick "nessuna azione" + scheduler:status "torneo FINITO" (F7 live) → **riavvio tournament:start RIPASSATO dal gate** (export_path valorizzato → reset DB, piattaforma intatta) (F3 live). Nota: OpenRouter free rate-limit fluttuante durante la run (429/rete su mistral-nemo e gemma): qualche invio email best-effort è fallito con warn (winner notification) — il flusso engine NON ne è stato impattato; casella di sistema ripulita (0 non lette, marcate le email di sistema). Artefatti throwaway rimossi (DB/env/export). **Rotazione App Password Gmail raccomandata** (esposta nel working tree fino alla cancellazione di `.check-inbox.mjs`).

- `2026-08-23T11:32:10Z` — **Taglio deterministico della citazione nelle reply email + separatore di sistema (piano email-reply-quote-stripping eseguito integralmente, TDD)**: risolve l'incidente UAT 2026-08-22 (classificatore LLM confuso dalla citazione della mail di sistema → pick registrati sbagliati: "cremonese pareggia"→US Catanzaro lose, "catanzaro"→SSC Bari win, "Catanzaro pareggia"→unrecognized — report §2). **Nuovo modulo puro `src/channel/email-adapter/reply-cleaner.ts`**: `extractPlayerReply(body)` normalizza CRLF→LF, taglia al PRIMO confine di citazione (separatore `SYSTEM_EMAIL_SEPARATOR='───'` con eventuale prefisso `>` e spazi / riga che inizia con `>` / marker "ha scritto:"/"wrote:" — incluso lo spezzamento reale Gmail "…ha"+"scritto:" su due righe), tiene le prime 5 righe NON vuote, fallback al body originale trimmato se il taglio produrrebbe una stringa vuota (D6: mai peggiorare il comportamento attuale). Costante `SYSTEM_EMAIL_SEPARATOR` PROVVISORIA ed ESPORTATA: unica per invio e ricezione. **Wiring**: `message-router.classify` applica `extractPlayerReply` DOPO il check corpo/mittente vuoto→unknown (il classificatore LLM riceve solo il testo del giocatore); `EmailAdapter.sendMessage` antepone il separatore a OGNI email di sistema PRIMA del banner TEST MODE (punto unico, mai nei template). **Test**: 18 nuovi (17 unit del cleaner con i 3 body REALI verbatim UID 291/293/295 + caso marker spezzato; 1 router citazione→pulita) + adapter aggiornato (separatore+banner nelle 2 modalità) — **523 verdi, typecheck/lint puliti**. **Accettazione verificata dal vivo** (`llm:classify`, `ENV_FILE=.env.uat`, mistral-nemo primario): UID 291→`US Cremonese draw`, UID 293→`US Catanzaro` win, UID 295→`US Catanzaro draw` — i tre esiti attesi del piano. Nessuna modifica a intent-classifier/email-renderer (scelta: pre-processing + separatore, non contenuto delle mail). Rischio dichiarato: pick oltre la 5ª riga non vuota → perso (il sistema chiede chiarimento, comportamento sicuro).

- `2026-08-22T13:20:00Z` — **UAT in MODALITÀ SCHEDULER (SCHEDULER_ENABLED=true) completata — chiusura automatica end-to-end**: run compressa (~13') su DB freschi (seed 4 squadre 2 giornate `--first-kickoff-offset-min 8 --spacing-min 6`, `DEADLINE_ADVANCE_MIN=3`, `MATCH_DURATION_MIN=2`, `TC_CLOSE_SKEW_MIN=1`) con **modello LLM primario `mistralai/mistral-nemo`** (verificato dal vivo: parse pick + classificatore intento OK; failover invariati). Cron simulato con loop background (scheduler:tick + channel:email:process ogni 60s, come la doppia riga crontab). **Sequenza automatica completa**: round_open TC1 (13:05) → 2 pick `auto_joined` (profili via RF-P5) → round_close TC1 a deadline (13:10) → round_score TC1 (2/2 correct) → round_open TC2 (13:13) → pick R2 di Fulvio `pick_registered` (13:15) → round_close TC2 (13:16) con Pippi eliminata per `missing_pick` (pick arrivato 13:15:40 entro deadline ma processato al tick successivo a round chiuso → `round_not_open`, comportamento corretto) → **CHIUSURA AUTOMATICA**: vincitore Fulvio (caso 1), export `data/exports/tournament-export-2026-08-22T13-16-48.121Z.json`, scheduler inibito ("Prossime azioni: nessuna"). Nota: con finestre pick ultra-compresse (3') e tick 60s un pick a ridosso della deadline può non essere processato in tempo — irrilevante coi tempi reali. `.env.uat`: `SCHEDULER_ENABLED=true` (per la run) + parametri compressi + `LLM_MODEL` aggiornato (mistral-nemo primario).

- `2026-08-22T12:55:00Z` — **UAT end-to-end RIUSCITA (dopo il fix guardia round:open)**: nuova run su DB freschi (`uat-synthetic-pippo.db`/`uat-platform-pippo.db`, seed 4 squadre 2 giornate `--first-kickoff-offset-min 60`, `DEADLINE_ADVANCE_MIN=30`) con i 2 giocatori reali Fulvio De Giovanni e Pippi. Flusso completo verificato dal vivo: `tournament:start` → `round:open` R1 (guardia passa: deadline futura) → 2 email pick processate `auto_joined` (auto-join RF-P5: profilo+pick atomici, 2 profili creati) → `round:close --force` + `round:score` R1 (2/2 pick `correct`) → `round:open` R2 (2 profili notificati) → 2 email pick `pick_registered` → `round:close --force` + `round:score` R2 (Fulvio `wrong_pick` eliminato, Pippi `correct`) → **CHIUSURA AUTOMATICA del torneo** (ADR-011): `winner_notified=1`/`finished_at` 12:52:04.171Z, notifica vincitrice, export automatico `data/exports/tournament-export-2026-08-22T12-52-04.171Z.json` (dump completo: tournament_state/profiles/picks), scheduler inibito ("Prossime azioni: nessuna"). Chiuso il gap dimostrativo dell'incidente mattutino (0 pick/0 profili per apertura a deadline scaduta).

- `2026-08-22T12:40:00Z` — **Lock file su `channel:email:process` (anti-concorrenza cron)**: con `SCHEDULER_ENABLED=true` il cron lancia il comando ogni minuto; un run può superare il minuto (worst-case LLM ~135 s per messaggio + retry `markSeen`), e due processi sovrapposti leggevano le stesse email non lette (`BODY.PEEK[]`, `\Seen` solo a fine processing) producendo risposte duplicate/contraddittorie (pick già registrato → `pick_rejected`; subscribe → eccezione su `UNIQUE(email)` → terza email al tick successivo). **Fix**: nuovo modulo puro `src/cli/email-process-lock.ts` (pattern logica-in-modulo/handler-sottile, zero dipendenze): `lockPathFor(dbPath)` deriva `<dir>/<nome DB>.lock` da `DB_PATH` (percorsi diversi → lock diversi → UAT e produzione non collidono; `data/` gitignorata), `acquireLock` con creazione esclusiva atomica `'wx'` (mkdir ricorsivo come `createConnection`), contenuto = PID proprietario, staleness via `process.kill(pid, 0)` (`ESRCH` → morto → steal con un solo secondo tentativo; `EPERM` → vivo), fallback sicurezza: contenuto corrotto + mtime oltre `STALE_MS` (10 min) → stantio, corrotto + fresco → skip; `touchLock` periodico ogni `TOUCH_INTERVAL_MS` (30 s, `setInterval` + `unref()`) così un run lungo non appare mai stantio; `releaseLock` rimuove il file SOLO se il PID coincide ancora (no-op sicuro). **Wiring** nel solo handler `channel:email:process`: lock dopo `getConfig()` e prima dei DB; lock occupato → log pino info in INGLESE (`email:process skipped: another instance is running…` con `lockPath`/`holderPid`), banner TEST MODE + messaggio italiano nel ramo testo (`Processo email già in esecuzione (lock …, pid …): esecuzione saltata`), `--json` → `{skipped: true, lockPath, holderPid}` via `jsonWithTestMode`, exit 0 (il cron non si allarma); `clearInterval` + `releaseLock` nel `finally` esistente. `channel:email:fetch`/`send`, scheduler, comandi round/tournament/simulate NON toccati (read-only o idempotenti). **Test**: `tests/unit/cli/email-process-lock.test.ts` (7 test), suite completa verde. con `SCHEDULER_ENABLED=true` il cron lancia il comando ogni minuto; un run può superare il minuto (worst-case LLM ~135 s per messaggio + retry `markSeen`), e due processi sovrapposti leggevano le stesse email non lette (`BODY.PEEK[]`, `\Seen` solo a fine processing) producendo risposte duplicate/contraddittorie (pick già registrato → `pick_rejected`; subscribe → eccezione su `UNIQUE(email)` → terza email al tick successivo). **Fix**: nuovo modulo puro `src/cli/email-process-lock.ts` (pattern logica-in-modulo/handler-sottile, zero dipendenze): `lockPathFor(dbPath)` deriva `<dir>/<nome DB>.lock` da `DB_PATH` (percorsi diversi → lock diversi → UAT e produzione non collidono; `data/` gitignorata), `acquireLock` con creazione esclusiva atomica `'wx'` (mkdir ricorsivo come `createConnection`), contenuto = PID proprietario, staleness via `process.kill(pid, 0)` (`ESRCH` → morto → steal con un solo secondo tentativo; `EPERM` → vivo), fallback sicurezza: contenuto corrotto + mtime oltre `STALE_MS` (10 min) → stantio, corrotto + fresco → skip; `touchLock` periodico ogni `TOUCH_INTERVAL_MS` (30 s, `setInterval` + `unref()`) così un run lungo non appare mai stantio; `releaseLock` rimuove il file SOLO se il PID coincide ancora (no-op sicuro). **Wiring** nel solo handler `channel:email:process`: lock dopo `getConfig()` e prima dei DB; lock occupato → log pino info in INGLESE (`email:process skipped: another instance is running…` con `lockPath`/`holderPid`), banner TEST MODE + messaggio italiano nel ramo testo (`Processo email già in esecuzione (lock …, pid …): esecuzione saltata`), `--json` → `{skipped: true, lockPath, holderPid}` via `jsonWithTestMode`, exit 0 (il cron non si allarma); `clearInterval` + `releaseLock` nel `finally` esistente. `channel:email:fetch`/`send`, scheduler, comandi round/tournament/simulate NON toccati (read-only o idempotenti). **Test**: `tests/unit/cli/email-process-lock.test.ts` (10 test su `mkdtempSync`, mai sulla `data/` reale: derivazione path, isolamento tra DB diversi, acquire+directory padre, secondo acquire → null, release+riacquisto, steal su PID morto, corrotto+vecchio → steal, corrotto+fresco → null, touch aggiorna mtime, release no-op). **Smoke test CLI end-to-end** (senza rete): lock vivo → skip in JSON e testo con exit 0 e lock del proprietario MAI rimosso; PID morto → steal e proseguimento (fallito su credenziali dummy, atteso). Suite completa: **505 test verdi**, typecheck e lint puliti. Nota operativa documentata nell'header del modulo: dopo un reset dei DB il `.lock` residuo viene rubato automaticamente (PID morto); per igiene conviene eliminarlo insieme al DB; un PID riusato è un caso rarissimo (il fallback mtime resta inattivo finché il file non è rimosso a mano).

- `2026-08-22T13:45:00Z` — **Guardia `round:open` contro l'apertura con deadline già scaduta (fix incidente UAT 2026-08-22)**: diagnosi completa dell'UAT fallita su `data/uat-synthetic-pippo.db` — il round 1 era stato aperto alle 10:57:12Z con deadline 10:56:48Z (24s DOPO la deadline, `DEADLINE_ADVANCE_MIN=2` compresso a mano nel `.env.uat` vs 30 documentato): ogni pick successivo veniva rifiutato dal gate RF-31 (`after_acceptance`/`after_kickoff`) e l'auto-join faceva ROLLBACK (0 player/profile/pick nel DB, giocatori invitati all'impossibile, 2 account piattaforma `active` senza profilo). **Fix**: guardia in `openRound` (src/game/round-manager.ts) che rifiuta con `Deadline del round N non futura (…): apertura rifiutata (nessun pick sarebbe accettabile)` quando `now >= deadline`, PRIMA di ogni scrittura (nessuno stato parziale) — stesso stile del gate RF-21 di `tournament:start`; lo scheduler non è impattato (apre i round con kickoff sempre futuri). 3 test nuovi in `tests/integration/round-flow.test.ts` (dopo deadline → rifiuto senza round_state; esattamente alla deadline → rifiuto; prima → apertura normale). **Correzioni correlate**: `.env.uat` riallineato ai valori documentati della guida (`DEADLINE_ADVANCE_MIN=30`, `TC_CLOSE_SKEW_MIN=10`, `MATCH_DURATION_MIN=5` — i commenti stale indicavano valori diversi dai reali), nota operativa in `docs/uat/guida-test-mode.md` §2 (guardia + "non forzare, rifai il seed"), cli-reference `round:open` aggiornato, `docs/technical-administrator-manual.md` invariato (non cita la guardia; valutare in seguito). Suite completa: **495 test verdi**, typecheck e lint puliti.


- `2026-08-22T10:30:00Z` — **Prodotta la documentazione operativa per l'amministratore tecnico (2 documenti, solo docs, nessuna modifica al codice)**: `docs/technical-administrator-manual.md` (manuale in INGLESE — traduzione italiana prevista in un passo successivo — per amministratori VPS shell-literate, scope strettamente operativo: TOC, introduzione/regole di gioco/attori di sistema, matrice notifiche, acquisizione dati football-data.org con trigger di fetch, modalità operative commissioner/scheduler con doppia riga crontab, test mode condensato con rimando alla guida, CLI core operativa (21 comandi), ciclo di vita end-to-end dalla setup alla chiusura automatica e riavvio, configurazione completa .env con ENV_FILE/TIMEZONE/UTC semantics, modello temporale (deadline fissa, istante di accettazione min(deadline, kickoff), finestra TC, confine girone floor(N/2)+1, Freeze, esempi numerici production e UAT compresso) e glossario dei termini italiani) e `docs/cli-reference.md` (riferimento completo dei 34 comandi CLI stile man-page, con opzioni/default/guardie e messaggi verbatim). Scelte confermate dall'utente via domande: audience = admin VPS shell-literate; CLI reference in documento SEPARATO con core operativa nel manuale; percorso `docs/technical-administrator-manual.md`; test mode condensato + rimando alla guida; nessun riferimento RF/ADR nei testi. Verifica di accuratezza sui sorgenti reali (src/cli/commands/*, src/config.ts, src/game/round-time.ts, src/game/scheduler.ts) e sui documenti (PRD/HLD/guida test-mode/timeline-example).

- `2026-08-21T20:30:00Z` — **Piano `.kilo/plans/1787340283469-review-findings-fix.md` eseguito integralmente (fix findings revisione ADR-011, branch `feat/email-templates-v2`, working tree NON committato — nessun commit/push)**: **HIGH-2** numeri di turno rimossi dal prompt (`serializeEmailContext` serializza le bruciate SENZA round; il round di utilizzo resta solo nel box del renderer) + test generator reso bloccante con `burnedTeams`; **HIGH-1 + §1.3** seam di archiviazione (`GameContext.archiveTournament` iniettato, nuovo `src/cli/archive-wiring.ts` con `archiveTournamentFile`/`makeArchiveTournament`/`attachArchiveToContext`, MAI `node:fs` nei moduli di gioco), colonna additiva `tournament_state.export_path` + gate di riavvio (`tournament:start` rifiuta se chiuso senza export archiviato) + reset `export_path=NULL`; **MEDIUM-1/2** `isTournamentClosed` + `round:open` rifiuta a torneo chiuso + `round:score` aggiorna lo stato DB ma tace le email di esito (`round_result_*`, `pick_postponed`, `round_closed_survived`); **LOW-1** notifica eliminazione in `closeRound` best-effort (try/catch, warn inglese); **LOW-2** sonda timezone con locale neutro `en-US` + nota full-icu; **LOW-3** countdown `deadlineRemaining` solo se `now < deadline`; **LOW-4** rimosso campo morto `EmailMatchContext.date`; **Dev-8** prompt non ripete il saluto; **documentali** (header `llm.ts`, commento `turn.ts` turnCompact "per log/CLI", `--available-teams` in POC_LLD §7.8, "(la prima delle due righe)" in guida test-mode, ADR-012 emendamento post-revisione). Test aggiornati/aggiunti in `tournament-closure.test.ts` (harness con seam, filename via `exportFilename`, gate export fallito→riavvio rifiutato, round:open/score dopo chiusura), `generator.test.ts`, `email-renderer.test.ts`. Suite/typecheck/lint verdi.

- `2026-08-21T19:00:00Z` — **Piano `.kilo/plans/1787325393233-email-templates-v2.md` eseguito integralmente (Task 1–10, ADR-011, branch `feat/email-templates-v2`, working tree NON committato)**: **Task 1** ADR-011 registrata (resa = canale/dati = canale-agnostici, stile unico energetic, soggetti umani, nome da RF-P1, chiusura automatica completa, `TIMEZONE`) + emendamenti PRD RF-P1/RF-P6/RF-18/RF-26, LLD §6.2/§6.3/§6.4/§6.6/§7.7 e guida test-mode (nota operativa: rimozione riga crontab a torneo chiuso + riavvio); **Task 2** `formatRemaining` puro in `src/game/round-time.ts` (countdown "20 ore e 15 minuti", singolare/plurale, <1h/<24h/≥24h) + test; **Task 3** `EmailType`+`clarification`, `EmailContext` esteso (round/championshipRound/roundStart/deadlineRemaining/matches/burnedTeams/conteggi/playerResult), `subjectFor` in forma UMANA ("Survivor League — Round N · Turno di campionato M: etichetta", soggetti NEUTRI per gli esiti), `OpenAIGenerator({client, timeZone})`, `templates.ts` riscritto (16 prompt narrativa-only, `UNSUBSCRIBE_CONFIRM_WORDS` invariato) con `templates.old.ts` rinominato (file morto), `TIMEZONE` (IANA, default Europe/Rome, validata al boot) + `TOURNAMENT_EXPORT_DIR` in config e `.env*.example`, `createLogger(..., timeZone?)` con timestamp pino nel fuso (offset esplicito, default UTC storico) e call-site CLI aggiornati; **Task 4** `src/llm/email-renderer.ts` (header umano, box ASCII esito ✅/❌ con testi esatti, deadline+countdown elemento n.1, bruciate con round di utilizzo, partite/risultati, stato aggregato solo conteggi, CTA per tipo, chiusura fissa eliminato "Grazie per essere stato con noi!", mail torneo = solo annuncio) + test unit (box esatti, omissioni, fuso iniettato); **Task 5** Round Manager + Tournament popolati (open: bruciate+countdown+partite+roundStart; close: playerResult missing+inGameCount; score: esiti+risultati+stato aggregato+prossima deadline; TT1 RF-P6: `playerName = account.name ?? email`; `tournament_open` solo annuncio con `platformCount`); **Task 6** nome end-to-end (`platform_account.name` migrazione additiva, `PlatformAccount.name`, `register(email, name, now)`, CLI `platform:register --name`, classificatore `{intent, pick, name?}` con formula "dimmi il tuo nome e scrivi voglio iscrivermi", auto-join `player.name = account.name ?? email`); **Task 7** email di chiarimento con tipo `clarification` (soggetto "Non ho capito", 3 opzioni + formula col nome, box deadline se round aperto), `pick_confirmed`/`pick_rejected` con round/deadline/countdown; **Task 8** chiusura automatica completa: `settleWinnerIfNeeded` dopo `closeRound`/`scoreRound` (guardia atomica idempotente `winner_notified`/`finished_at` — migrazioni additive, notifica `tournament_won`/`tournament_shared_win` best-effort con filtro active, export automatico riuso `tournamentExport` in `TOURNAMENT_EXPORT_DIR` con filename dal clock RNF1, `computeActions` → [] a torneo chiuso + `scheduler:status` `tournamentFinished`, `tournament:start` riammissibile con reset atomico del DB di gioco e piattaforma intatta) + test dedicati (casi 1/3, doppio score non duplica, vincitore non active, scheduler fermo, riavvio/rifiuto); **Task 9** verifica manuale: simulazione completa su DB scratch (export automatico scritto, `tournamentFinished: true`, `nextActions: []`), generazione reale via LLM dei 4 casi chiave (istruzioni pick con box deadline, esito eliminato con box ❌ e chiusura fissa, apertura torneo solo annuncio, soggetti umani), prova `TIMEZONE=America/New_York` (15:30Z → 11:30 nel box); **Task 10** questo changelog + `git status`/`git diff` verificati prima del commit. **478 test verdi, typecheck/lint puliti.**
- `2026-08-21T11:45:00Z` — **Piano test-e-fix dei findings della revisione tecnica 2026-08-21 eseguito integralmente (B1–B8, branch `feat/iscrizione-piattaforma`, working tree NON committato — nessun commit/push)**: Fase A con test di riproduzione TDD (A1–A9) e Fase B in ordine HIGH→MEDIUM→LOW→documentale, ogni step chiuso a protocollo verde (suite intera + typecheck + lint). **B1** barriera unsubscribe intento-agnostica in `email-processor.ts` (pending_unsubscribe + body di conferma → soft-delete anche con intento `other`, D1/D2) + prompt classificatore con "confermo"/"sì"/"si"; **B2** `summary_sent` scritto PRIMA del loop di invio nella stessa UPDATE atomica della transizione a `scored` + invio best-effort per destinatario con warn pino in inglese (`GameContext.logger` + iniezione nelle CLI round/scheduler); **B3** `isAccountActive` fail-closed senza registry (D4); **B4** `PLATFORM_DB_PATH_DEFAULT` esportato da `config.ts` come fonte unica e guardia `simulate:*` ancorata ad esso (D8), smoke `simulate:full` su DB `/tmp/kilo` (rifiuto sul valore di produzione + run completo con `register_id` 1..10, `summary_sent=1`); **B5** ramo `other` risponde solo ad account `active` (silenzio+seen per `unsubscribed`/`pending_unsubscribe`, istanza D3); **B6** nuovo `EmailType` `platform_already_registered` ("Già iscritto alla piattaforma") per il subscribe su account già active; **B7** `autoJoinFromPick` riusa un `player` legacy senza `profile` con backfill `register_id` (nessun `already_registered` permanente); **B8a** dead-write `registration_open` rimosso da `startTournament` (colonna DEPRECATA, resta al default 0); **B8b** commenti stale corretti (tournament.ts, pick-processor.ts "auto-join RF-P5", scheduler.ts describe, pick.ts `makeNow(config)`, generator.test.ts 14→15); **B8c** contract test `llm:classify` (A9, nuovo `tests/unit/cli/llm-classify.test.ts`, 7 test, VERDE da subito: gap di copertura D7, nessuna modifica al comando); **B8d** ADR-010 registrata (carve-out conferme RF-P1/P2 al filtro `active`, semantica barriera allineata alla decisione 3, decisioni (a)–(g) formalizzate) + PRD v0.6.1 (RF-P6 carve-out, RF-P1 con `platform_already_registered`, changelog) + LLD §6.3 aggiornato; **B8e** conteggio test corretto. **Test: 414 (baseline) → 431 verdi** (+17: A1, A2, A3, A4, A5, A6a, A6b, A7, +1 contract automatico del nuovo tipo, A8, A9×7), typecheck/lint puliti. Grep finali di conformità OK (nessuna scrittura via `ctx.platform` fuori dal registry, nessun `registration_open = 1` in src/, nessuna "finestra di iscrizione" in scheduler.ts, nessun `RF-27` in pick-processor.ts, log pino nuovi in inglese).

- `2026-08-20T19:00:00Z` — **Piano `tasks/iscrizione-piattaforma/plan-iscrizione-piattaforma.md` eseguito integralmente (Task 1–11, ADR-009, branch `feat/iscrizione-piattaforma`)**: Task 1 ADR-009 registrata (modello a due livelli, storage separato, soft-delete a due passi, silenzio anti-spam, intento LLM, auto-join TT1, matrice notifiche, deprecazioni); Task 2–4 documenti allineati (PRD v0.6.0 con RF-P1…P8/US1/US7/US8 riscritte e US10 senza iscrizione manuale, CL2/CL5/CL10/CL16 riscritti; HLD v0.5.0 con Platform Registry + Intent Classifier; LLD v0.5.0 con `platform_account`, colonne additive `register_id`/`summary_sent`, interfacce §6.2/§6.6, comandi §7); Task 5 `PLATFORM_DB_PATH` (config + `.env*`), `src/db/platform-schema.ts`, `src/platform/registry.ts` (register/beginUnsubscribe/confirmUnsubscribe/reactivate/unregister/find/activeEmails/list, clock iniettato RF-P8), CLI `platform:migrate/register/unregister/list`; Task 6 `src/llm/intent-classifier.ts` (intento+pick in UNA chiamata, filtro esatto, `other`/`pick:null` su contenuto, LLMError su trasporto), `OpenAIParser` delegato al classificatore, `EmailType` nuovi (`platform_registered`/`platform_unsubscribe_confirm`/`platform_unsubscribed`/`tournament_open`/`round_closed_survived`; rimossi `welcome`/`registration_open_invite`/`auto_registered`), CLI `llm:classify`; Task 7 `GameContext.platform`/`classifier`, `checkEligibility` = account `active`, `autoJoinFromPick` (RF-P5, `register_id` replicato, rollback senza profilo), stub `@deprecated` registerPlayer/open/close/autoRegisterFromPick, colonne additive `player.register_id`/`profile.register_id`/`round_state.summary_sent`, `pick:register` verifica account active; Task 8 router senza `REGISTRATION_KEYWORDS` (`{kind:'classified'}`) + email-processor riscritto (subscribe/unsubscribe a due passi con lista conferma `confermo`/`sì`/`si`/`yes`, silenzio anti-spam, auto-join/post-TT1, altri→chiarimento/silenzio, mittenti rivalutati per messaggio HIGH-2, stop batch su LLMError), `channel:email:process` migra ENTRAMBI i DB; Task 9 notifiche filtrate su account `active` (round open/close/score, broadcast `tournament_open` in `startTournament`, riepilogo `round_closed_survived` ai soli sopravvissuti alla transizione `closed→scored` con guardia `summary_sent`, nessun `round_closed_eliminated`), `tournament:status`/`scheduler:status` senza `registrationOpen` con `platformSubscribers`, wiring `attachPlatformToContext`; Task 10 scheduler senza azioni `register_close_auto/safety` e senza stub deprecati, simulazione riscritta (account piattaforma + auto-join al TT1, guardie: `PLATFORM_DB_PATH` ≠ produzione e DB piattaforma pulito), `PLATFORM_DB_PATH` dedicato in `.env.uat`/`.env.uat-replay` e negli esempi; Task 11 `.env` con `PLATFORM_DB_PATH`, mappa documenti AGENTS.md, guida test-mode e system-components aggiornati. **429 test verdi**, typecheck/lint puliti a ogni task. Smoke CLI su DB `/tmp` dedicati: subscribe→pick→close→score con riepilogo unico; `simulate:full` deterministico a parità di seed con DB piattaforma pulito; guardia anti-produzione verificata.

- `2026-08-18T10:01:00Z` — **Pubblicazione iniziale su GitHub (setup git + primo commit + push su main)**: identità Git globale configurata (`user.name=fulviodeg`, `user.email=78381446+fulviodeg@users.noreply.github.com` — email noreply di GitHub — e `init.defaultBranch=main`); `.gitignore` potenziato (build, `*.log`, `*.tsbuildinfo`, `.tmp/`, `coverage/`, file OS/IDE, `note personali.txt`; mantenuti i 4 `.env` reali esclusi; i template `.env*.example` restano tracciati); primo commit `515a72d chore: initial commit` (191 file) + `05e8318 docs: add README and CHANGELOG` (creati `README.md` e `CHANGELOG.md`); chiave SSH ed25519 generata in `~/.ssh/id_ed25519` con `~/.ssh/config` dedicato a `github.com` e autenticazione verificata (`ssh -T git@github.com` → "Hi fulviodeg!"); repository **privato** `github.com/fulviodeg/SurvivorLeague` creato e `main` pubblicato con `git push -u origin main`. Nota test: 139/140 verdi — 1 fallito solo per ICU "small" del node in sandbox (locale `it-IT` assente), non un bug del progetto.

- `2026-08-18T09:21:35Z` — **Guida test-mode aggiornata con l'aggancio asincrono (ADR-008/RF-20…31)** — `docs/uat/guida-test-mode.md` ora documenta l'avvio del torneo da un TC arbitrario come **funzionalità di sistema** collaudabile in test mode (solo documentazione, nessuna modifica al codice). Aggiornamenti distribuiti nelle sezioni esistenti (nessuna sezione nuova autonoma): header **Scope** + §1.1 (paragrafo in linguaggio semplice con i due concetti `TT = TC − start_round + 1` e reset del pool al confine `floor(N/2)+1`, distinzione esplicita "funzionalità di sistema, non del test mode"); §2.5 (nota `tournament:start --start-round <n>`, default `1`) e §3.1 (flusso aggiornato a `tournament:start [--start-round <n>]` + verifica via `tournament:status` riga `start TC …`/`tournament:history` righe `TTnTCm`); §4 (nota: `--rounds` = dominio dei TC agganciabili `1..rounds`, confine calcolato su quel totale, calendario MISTO incoerente perché `totalRounds = MAX(round)`); **nuovo esempio §5.4** "Aggancio asincrono — 8 squadre, 6 giornate, avvio da TC 3" (`--teams 8 --rounds 6`, `tournament:start --start-round 3` → output atteso `Stagione avviata: TT1 = TC 3, 4 round inizializzati (confine girone 4)`, giornate 1–2 non giocate, pool reset a TC 4 = TT 2, vincolo RF-21 ~120 min dal seed, dry-run `simulate:full --start-round 3`); §6.1/§6.2 (aggancio asincrono RF-20/mappatura TT-TC/reset pool dimostrabili; confine reale TC 20 e aggancio oltre `rounds` non dimostrabili); §7 (nota replay 2025: `--start-round <n>` in `1..38` incluso TC 20, `TEST_OFFSET_DAYS` da tarare per deadline futura); §9 glossario (nuove voci **Aggancio asincrono**, **start-round / TC di aggancio**, **TTnTCm** + voce **TC/TT** completata con `TT = TC − start_round + 1`). **Verifica:** grep dei comandi/output sul codice (tutti confermati) + dry-run su DB separati `/tmp/kilo/uat-hook-test.db` (seed 8×6 → `tournament:start --start-round 3` → output/status esatti) e `/tmp/kilo/uat-hook-sim.db` (`simulate:full --start-round 3` → `TT1 = TC 3 (6 TC)`, 4 round TC 3–6, mappatura TT/TC corretta).

- `2026-08-18T09:35:00+02:00` — **Piano UAT Task 6 implementato (guida operativa `docs/uat/guida-test-mode.md`)**: documento di riferimento durevole in italiano per amministratori/PO/operatori NON developer e agenti, "libretto di istruzioni" del TEST_MODE. Documenta l'implementazione REALE (Task 0.x–5): attivazione via `ENV_FILE=.env.uat` (`TEST_MODE=true`); cosa cambia per componente (loader/config con `ENV_FILE` e parametri test-only; banner `[TEST MODE] This email was sent by a test instance of Survivor League.` sulle email inviate; dicitura `TEST MODE` in CLI + campo `testMode` nei `--json`; binding `testMode:true` nei log pino; Parser con risorsa alias sintetica Serie B `team-aliases-synthetic.md` + contesto lega nel prompt; clock/receivedAt spostabili solo con `TEST_OFFSET_DAYS>0` per replay; scheduler con refresh disattivabile `TEST_REFRESH_ALLOWED`); tabella parametri test con motivazione; gating a consumo §0.3. Manuale operativo completo (attiva/usa/disattiva/verifica, cosa fare e NON fare, procedure standard con comandi di verifica per fase). Entrambe le modalità: commissioner (manuale seed→start→open→pick→close→score, chiusura forzata RF-29) e cron (`scheduler:tick` + `channel:email:process` su due righe crontab — `scheduler:tick` NON legge la casella; vincolo mai `data:refresh` su DB sintetico). Sezione seed in linguaggio semplice (`--force`/`--clear`, calendario MISTO, guardia stato di gioco, WARN fuori test mode, log error sovrapposizione D8). TRE timeline con liste comandi copiabili e valori prescritti: 2h (`--teams 4 --rounds 2 --spacing-min 45 --first-kickoff-offset-min 60`, ≈1h45, finestre 30'/45'), ~4h30 (`--teams 8 --rounds 6 ...`, ≈4h45), ~6h30 (`--teams 8 --rounds 8 ...`, ≈6h15, round 8 = wrap round-robin spiegato). Sezione scope con NOTA RF-31 (`after_kickoff` solo commissioner/CL17; nel cron un pick tardivo riceve `round_not_open`). Assunzione giocatori di TEST dichiarata all'inizio e negli esempi. Replay 2025 (`.env.uat-replay`, `TEST_OFFSET_DAYS=365`, DB dedicato, nota diagnostica fetch raw vs process shiftato). Cleanup casella Gmail condivisa (riconoscimento email di test via banner). Glossario non-tecnico. **Verifica con evidenza** su DB di scarto `data/guide-test.db` (modalità commissioner, 0 giocatori → 0 email reali, transizioni immediate con `round:close --force --reason`): migrate→seed (3 esempi: 4/2/45/60, 8/6/45/60, 8/8/45/60 con wrap R8=R1 accoppiamenti)→register:open→start→round:open (notificati 0)→round:close --force→round:score (scored immediato, punteggi pre-seedati)→status/deadline/results/leaderboard/history/export/scheduler:status, tutto senza errori; `testMode:true` in `--json` e nei log; refresh skip loggato `import/refresh skipped: TEST MODE...`; WARN fuori test mode `data:seed-synthetic is a test-only command...`; log error sovrapposizione con `--spacing-min 5`. Coerenza nomi: 24 comandi CLI citati + opzioni seed/round/register + 10 parametri env tutti verificati vs `src/cli/commands/` e `.env.uat.example`/`src/config.ts` (messaggi inglesi citati verbatim). Sanity: typecheck/lint puliti, **374 test su 35 file verdi** (nessun codice toccato). Guida collegata nella mappa documenti AGENTS.md §1.6. Review indipendente eseguita via Agent Manager ("review task 6 guida uat", DeepSeek V4 Pro, read-only): verdetto iniziale FAIL per 1 difetto bloccante (§5.3 confine di girone "dopo il round 7" → corretto in round 5 = `floor(8/2)+1`, decouplando wrap e reset del pool, allineato a `src/game/rules.ts:58-59` e all'output reale `confine girone 5`); riesecuzione della lista §5.3 su DB di scarto completata senza errori. 2 osservazioni minori non bloccanti lasciate invariate (etichette durata ~4h30/~6h30 vs ≈4h45/≈6h15, intentional dalle figure prescritte; `tournament:status` "Iscrizioni: aperte" dopo `register:close`+`start`, comportamento engine fuori scope guida).

- `2026-08-17T19:25:00+02:00` — **Piano UAT Task 4 implementato (guardia refresh/import in test mode)**: con `TEST_MODE=true` e `TEST_REFRESH_ALLOWED` assente/falso (default), `data:refresh`/`data:import` e il refresh dello scheduler NON chiamano l'API né toccano la tabella `match` (output "import/refresh skipped: TEST MODE..." in inglese + log pino info); con `TEST_MODE=true` + `TEST_REFRESH_ALLOWED=true` eseguono normalmente con log WARN di consenso a ogni operazione, incluso il `DB_PATH` (campo strutturato `dbPath` + messaggio); con `TEST_MODE=false` `TEST_REFRESH_ALLOWED` è ignorato (gating a consumo §0.3), comportamento invariato. La guardia vive SOLO nei comandi CLI: `importMatchesWithGuard(db, client, config, logger)` esportata in `src/cli/commands/data.ts` e `buildRefreshForTick(db, client, config, logger)` in `src/cli/commands/scheduler.ts` — `importMatches` resta PURA (nessun `getConfig()` nei moduli) e `src/game/scheduler.ts` NON è stato toccato (con guardia attiva `deps.refresh` non viene passato e `refresh_failed` è irraggiungibile in test mode, comportamento atteso). Convenzione documentata nei commenti (AGENTS.md §5): ogni futura chiamata a `importMatches` dai comandi deve applicare la stessa guardia. TDD: `tests/unit/cli/data-refresh.test.ts` (4 test) e `tests/unit/cli/scheduler-tick.test.ts` (4 test). **363 test verdi**, typecheck/lint puliti. Prova manuale su DB disposabile `/tmp/guard-test.db`: skip loggato e `match_date` invariate con `TEST_MODE=true`; WARN di consenso con `dbPath=/tmp/guard-test.db` + import reale (380 partite) con `TEST_REFRESH_ALLOWED=true`. Review indipendente avviata via Agent Manager ("review task 4 uat").

- `2026-08-17T18:50:00+02:00` — **Piano UAT Task 2 implementato (`data:seed-synthetic`)** (Task 1 già completato e reviewato PASS nella sessione precedente): nuovo comando CLI che genera la stagione sintetica con `generateSyntheticSeason` (Task 1) e la carica nella tabella `match` con `upsertMatches` (stessa pipeline di `data:import`, ADR-007 "la CLI inietta"). Opzioni con default: `--teams 8` (nomi da `SYNTHETIC_TEAMS.slice(0, n)`, validato 2..8 con errore chiaro), `--rounds 7`, `--spacing-min 90`, `--first-kickoff-offset-min 120` (primo fischio = `Date.now() + offset`, clock REALE, `TEST_OFFSET_DAYS=0`), `--seed 42`, `--force`, `--clear`, `--json`. Orchestrazione testabile `seedSyntheticSeason(db, opts, config, logger)` esportata in `src/cli/commands/data.ts` (le guardie vivono nel comando, i moduli dati restano puri): gate test-only WARN pino (inglese, non blocco) con `TEST_MODE=false`; guardia anti-sovrascrittura D6 (rifiuto senza `--force` su tabella non vuota, nessuna modifica); `--clear` richiede `--force` (doppia conferma); `--force` senza `--clear` su tabella non vuota → WARN pino (inglese) di calendario MISTO (l'upsert non fa DELETE; `getTeams()`/`getTotalRounds()` incoerenti con la risorsa alias sintetica) + suggerimento `--clear` nell'output; `--force --clear` → guardia stato di gioco Task 3 (rifiuto se `tournament_state.season_started=1` o righe in `pick`/`round_state`), poi svuota SOLO `match` tramite nuovo helper isolato `clearMatches` (src/data/importer.ts — unico punto del livello dati con DELETE, commentato che l'import base non fa MAI DELETE) e ri-seeda; rilevazione sovrapposizione D8 (`--spacing-min < MATCH_DURATION_MIN + TC_CLOSE_SKEW_MIN`) → log `error` pino in INGLESE con suggerimento dei parametri coinvolti (log, NON blocco). Output riepilogo (squadre/giornate/partite, primo/ultimo fischio), `--json` via `jsonWithTestMode` e banner TEST MODE via `printTestModeBanner` nel ramo testo. TDD: `tests/integration/seed-synthetic.test.ts` (16 test su SQLite in-memory con log pino catturato). **355 test verdi** (339 + 16 nuovi), typecheck/lint puliti; prova manuale su DB di prova: seed → `data:calendar` mostra la stagione sintetica, guardie (`--clear` senza `--force`, anti-sovrascrittura, stato di gioco) e log `error` di overlap verificati da CLI. Review indipendente avviata via Agent Manager (DeepSeek V4 Pro, "review task 2 uat").

- `2026-08-17T09:55:00+02:00` — **Piano UAT Task 0.1-0.4 implementati (test mode: loader/config, segnalazione, offset unificato, risorsa alias sintetica), Checkpoint A**: la run `channel:email:process` del mattino ha mostrato 2/20 `markSeen` falliti (`AggregateError [ETIMEDOUT]` con messaggio vuoto in fase CONNECT — Gmail sotto churn di ~20 connessioni nuove, una per messaggio) → messaggi non letti con risposta già inviata → rischio risposte duplicate al tick successivo. Fix: `EmailAdapter.markSeen` ritenta l'operazione fino a `MARK_SEEN_ATTEMPTS=3` volte con connessione NUOVA per tentativo e pausa `MARK_SEEN_RETRY_DELAY_MS=1000` (il flag \Seen è idempotente: ritentare è sicuro); solo a esaurimento lancia `EmailAdapterError` (D7 invariato). Inoltre `imap-client.markSeen` ora verifica il valore di ritorno di `messageFlagsSet`: `false` (STORE rifiutato) → errore esplicito invece di fallimento silenzioso. 3 test nuovi (retry su connect transitorio → successo con nuova connessione; errore dopo 3 tentativi senza flag; STORE `false` → errore) → **280 test verdi**, typecheck/lint puliti. **Validazione stress reale**: batteria 20 email + `channel:email:process` su DB dedicato → **20/20 processati, 20/20 `seen` persistiti, 0 errori, esattamente 20 risposte (0 duplicati)**, cleanup 0 non lette. Nota osservata (non modificato, artefatto dell'ambiente di test dove il sistema scrive a sé stesso): le risposte del sistema con From = casella stessa vengono riprocessate come messaggi in ingresso (`round_not_open` → risposta a risposta); in produzione le risposte vanno ai giocatori e il loop non può verificarsi.
- `2026-08-16T06:47:06Z` — **Piano failover multi-modello LLM eseguito integralmente (Tasks 1–10)**: `LLM_MODEL` ora accetta una lista separata da virgola in ordine di priorità (zod → `string[]`: trim, scarto vuoti, dedup ordinato; lista vuota → `ConfigError`); nuovi `LLM_TIMEOUT_MS` (default 15000, D6) e `LLM_RETRIES` (default 3 = 1 richiesta + 2 ritentativi, D4). `OpenAIClient` (`model` → `models: string[]`): retry per modello SOLO su errori ritentabili (429/5xx/timeout/rete/body malformato, delay 1 s), 4xx deterministici → failover diretto, lista esaurita → `LLMError` aggregato con modelli/esiti + ultimo errore specifico (D5); failover MAI su `null` (D3); callback `onModelTried` collegata a pino nel wiring email (log inglesi, D7). **Deviazione documentata dal commento "MAI retry automatico in POC"**: retry configurati N intra-modello e failover tra modelli (D9). **Fix bug critico `markSeen`** (imap-client.ts): `mailboxOpen('INBOX')` prima di `messageFlagsSet` (il STORE senza SELECT falliva silenziosamente → flag \Seen mai persistito → risposte duplicate a ogni tick) + test di regressione sulla sequenza (fallisce senza il fix). **Stress test reale completato** (report `docs/uat/stress-test-report-2026-08-16.md`): drain 0; batteria canale 20/20 (UID 79–98); batteria parser 16/16 senza blocchi (CLI 11/16 attesi, trace con modello vincente 14/16; primario vincente sempre `nvidia/nemotron-3-super-120b-a12b:free`, 0×429 in finestra); `channel:email:process` su `data/smoke-uat.db`: 20× `round_not_open`, **seen persistito verificato dal vivo** (21× SELECT, 20× UID STORE), 20 risposte reali, batch NON fermato (1 errore assorbito da retry), nessun duplicato; cleanup 0 non lette, DB e harness eliminati. Test: 261 → **277 verdi**, typecheck/lint puliti. **Proposta `.env`** (poi applicata su richiesta utente, con commenti sui parametri): `LLM_MODEL=nvidia/nemotron-3-super-120b-a12b:free,google/gemma-4-26b-a4b-it:free,openai/gpt-oss-20b:free`, `LLM_TIMEOUT_MS=15000`, `LLM_RETRIES=3` (gemma-4-31b, attuale, quasi sempre 429 → in coda se lo si vuole tenere).
- `2026-08-15T14:10:22Z` — **Piano UAT `tasks/uat/plan-uat-calendario-sintetico.md` revisionato dopo verifica di compatibilità con l'aggancio asincrono (RF-20…31), registrazione/pick concorrenti, chiusure forzate (RF-28/29/30) e controlli pick (RF-31)**: nessuna regressione rilevata (il seed scrive solo `match`, i moduli engine restano intatti). Introdotti i presidi concordati: opzione seed `--clear` (svuota `match` prima di seminare, richiede `--force`; `--force` senza `--clear` → WARN calendario misto, l'upsert non fa DELETE), WARN test-only quando `TEST_MODE=false`, guardia test "nessun `new Date()` nei comandi CLI", seam unico della trasformazione `receivedAt` + test dello shift esatto, errore esplicito per `ENV_FILE` inesistente, file env dedicato `.env.uat-replay` per lo scenario 3.1 (niente override inline), nota "`game/scheduler.ts` non modificato / `refresh_failed` irraggiungibile in test mode", requisiti guida Task 6 estesi (seed/`--clear`/calendario misto, replay 3.1, fetch raw). D6 e tabella rischi aggiornati. Nessuna modifica al codice.
- `2026-08-15T13:10:00Z` — **Report stress test pubblicato in `docs/uat/stress-test-report-2026-08-15.md` + ricerca online modelli LLM gratuiti con raccomandazione**: il report completo (probe, drain, batteria canale 20/20, batteria parser 14/16, Task 6 interrotto, bug critico `markSeen`) è ora nel repo (`docs/uat/`), non più solo in `/tmp/kilo`. Ricerca online (OpenRouter API 413 modelli / 19 free, GroqCloud, Cloudflare Workers AI, NVIDIA NIM, Gemini API, GitHub Models ritirato 2026-07-30): lista modelli free con supporto `response_format: json_object` — requisito bloccante del Parser che il modello attuale `nemotron-3-ultra-550b-a55b:free` NON soddisfa (coerente con i fallimenti LLMError della sessione). **Raccomandazione: `google/gemma-4-31b-it:free`** via OpenRouter (stessa base URL/chiave, solo cambio `LLM_MODEL` in `.env`): json_object supportato, italiano eccellente (140+ lingue), adozione altissima (Google DeepMind, disponibile anche su Cloudflare Workers AI/NVIDIA NIM/HuggingFace); alternative `openai/gpt-oss-20b:free` e `nvidia/nemotron-3-super-120b-a12b:free`. Da evitare `openrouter/free` (router casuale, viola determinismo RNF1). Nessuna modifica al codice.
- `2026-08-15T12:40:00Z` — **Stress test reale I/O (email channel + LLM parser) eseguito, esito parziale con bug CRITICO trovato**: harness temporaneo alla root (`smoke-send.mts`, poi eliminato) + plus-addressing Gmail. Esiti: **probe** — Gmail accetta il From plus-addressing ma lo RISCRIVE togliendo il tag (display name preservato) → fallback D1 (solo display name sull'indirizzo base; identità distinte non copribili dal vivo); **drain INBOX** 0 non lette; **batteria canale 20/20** inviate e ricevute con from/body/receivedAt coerenti (anomalie attese: corpo vuoto → fallback soggetto per #9, corpo+soggetto vuoti per #20, HTML come testo); **batteria parser 16/16** con modello reale: 14 attesi, 2 mismatch informativi (#10 "Lazio, 2" → null deterministico; #14 doppio pronostico → null, non primo pick; 3 chiamate fallite per rate-limit del modello free, riuscite al retry); **Task 6 INTERROTTO**: run `channel:email:process` su DB dedicato `data/smoke-uat.db` sempre fermata su `LLMError` (modello free instabile: risposte senza content testuale/JSON malformato, latenze >120 s) e — **BUG CRITICO** — `markSeen` (src/channel/email-adapter/imap-client.ts:80-81) chiama `messageFlagsSet` SENZA `mailboxOpen`: il handler STORE di imapflow (store.js:20) ritorna `false` silenziosamente con state≠SELECTED → flag \Seen MAI persistito (pur loggando successo D7) → a ogni tick i messaggi vengono riprocessati e le risposte duplicate reinviate (casella cresciuta da 20 a 55 non lette durante il test). Cleanup (D3) completato: 55 email marcate lette, `smoke-uat.db` e harness eliminati, fetch finale 0 non lette. Nessuna modifica al codice sorgente (fuori scope §8). Raccomandazioni: fix critico `markSeen` (aprire INBOX prima del STORE + test di regressione), modello LLM a pagamento/stabile per l'UAT e2e.
- `2026-08-15T09:20:00Z` — **Real-I/O smoke test (email channel + LLM parser) passed on real Gmail + OpenRouter**: previously the email adapter and the LLM parser had only been exercised with fakes/mocks (unit/integration tests), so the components were not yet proven against the real services. Executed with the real `.env`: `channel:email:send` sent a real email via Gmail SMTP to survivorleague755@gmail.com ("TEST SMOKE pick"); `channel:email:fetch` read it back via Gmail IMAP (receivedAt = internaldate, body intact, no flag set per D7); `llm:parse --input "scelgo la Juventus, vince"` returned `{team: "Juventus FC", outcome: "win"}` via the real OpenRouter model (nemotron-3-ultra-550b-a55b:free). Component-level I/O is now proven against real services; the full UAT (real players, tournament start, checklist CS1–CS7) remains to be planned with the commissioner. Note: the smoke-test email is still unread in the mailbox (D7: fetch marks nothing) and the mailbox also contains older unread messages (Google notifications). No code changes.
- `2026-08-15T07:00:00Z` — **Diagramma dei componenti: versione Excalidraw + fix Mermaid**: il blocco `componentDiagram` di `docs/system-components.md` non renderizzava nella preview (sintassi non supportata dal renderer locale), quindi è stato riscritto in `flowchart TB` con subgraph per layer e classDef colorate (stesso formato già usato in `POC_HLD.md`/`POC_PRD.md`); generato inoltre `docs/system-components.excalidraw` (235 elementi: 41 box componente, 7 bande layer, 45 relazioni, legenda) tramite script deterministico `/tmp/kilo/gen_excalidraw.py` — validato: JSON valido, id unici, nessun overlap tra box, font Excalifont ovunque. Nessuna modifica al codice.
- `2026-08-15T06:30:00Z` — **Prodotto `docs/system-components.md`**: vista grafica completa del sistema — component diagram UML (Mermaid `componentDiagram`) con tutti i componenti (CLI + wiring, Config/Logger/DB, Season Data ADR-007, Game Engine, LLM Adapter ADR-004, Channel email, servizi esterni), le relazioni tra componenti (~50 archi: uso/invocazione `-->` e realizzazione `::>`) e le funzioni implementate da ciascun componente (nei box e nel catalogo tabellare §3: 31 componenti, ruoli, funzioni chiave, dipendenze, riferimenti ai file sorgente). Legenda e note architetturali (GameContext DI, ADR-004/007/008, scheduler orchestratore, simulazione con clock deterministico). Nessuna modifica al codice.
- `2026-08-14T21:20:16Z` — **Brainstorming UAT pre-produzione documentato**: prodotto `tasks/uat/brainstorming-test-uat.md` (bozza di lavoro, lingua italiana, convenzione briefing) che raccoglie il brainstorming del 2026-08-14 col commissioner su come collaudare la POC end-to-end (engine + canale email + LLM + scheduler) prima del campionato 2026/27: problema dei due orologi (RF-21/RF-31 vs dati storici), 6 strade (3.1 test-mode clock finto, 3.2 stagione sintetica futura con date future, 3.3 dati reali 2026/27 da verificare subito, 3.4 date-shift replay 2025 con divieto di `data:refresh`, 3.5 UAT graduato a fasi, 3.6 casella Gmail test + cron in staging), tabella comparativa, raccomandazioni, allegato sullo stato del walkthrough CLI in corso (run 1 seed 42 archiviata, proseguire con `simulate:round --round 1 --seed 7`) e prossimi passi. Nessuna decisione normativa adottata: le raccomandazioni (verifica fixtures 2026/27, comando seed stagione sintetica) vanno confermate col PO/commissioner.
- `2026-08-14T19:50:21Z` — **Fase 7 completata (Checkpoint 5)** con le decisioni R1–R8 del briefing `tasks/briefing-fasi/briefing-fase-7.md`: **Decisione A** (fix determinismo RNF1: `created_at` di player/profile/pick scritto esplicitamente dal clock iniettato in `insertPendingPick`/`registerPlayer`/`autoRegisterFromPick`); Task 7.1 `src/game/simulation.ts` (mulberry32 puro, seed default 42, clock derivato dai dati per fase, guardia R3, contesto senza email) + CLI `simulate:full`/`simulate:round`; Task 7.2 `src/game/scheduler.ts` (`computeActions` pura, `schedulerTick` con `deps.refresh` iniettato e check-then-act idempotente, chiusure di sicurezza RF-30 con causa `deadline_missing`, `warn_not_calculable`, `schedulerStatus` computato — R5) + CLI `scheduler:tick` (log pino degli eventi, no-op se `SCHEDULER_ENABLED=false`) / `scheduler:status`; Task 7.3 esempio crontab in `.env.example` §4.4. 24 test nuovi (11 season-sim + 10 scheduler + 3 Decisione A; totale 261), typecheck/lint puliti. **UAT CLI eseguita senza mock:** `data:import` reale (380 partite) → `simulate:full --seed 42` → 38/38 TC scored (CS3) → doppio `tournament:export` identico salvo `exportedAt` (RNF1) → `--start-round 20` (CL13) e `--start-round 38` (CL12 con warning) → `simulate:round` + guardia R3 da CLI → `scheduler:status`/`scheduler:tick`; `tournament:start` su dati storici rifiuta per RF-21 (atteso). Correzioni documenti tabella §6 applicate: LLD §1.4 (evento `warn_not_calculable`), §3 (nota created_at/Decisione A), §5 (tree con `simulation.ts`/`scheduler.ts`), §7.11 (opzioni simulate + seed 42 + guardia), §7.12 (status computato R5); plan.md Fase 7 (R1–R8 + Decisione A); PRD §9 (nota RNF1).
- `2026-08-14T18:35:00Z` — **Fasi 5 e 6 completate (Checkpoint 4)** con le decisioni D1–D9 del briefing `tasks/briefing-fasi/briefing-fase-5-6.md` applicate: LLM Parser (`src/llm/parser.ts`, interfaccia con `{teams, aliases}` per chiamata, filtro esatto, `null` per contenuto / `LLMError` per trasporto), client condiviso (`src/llm/openai-client.ts`, `src/llm/errors.ts`), Generator con 12 template (`src/llm/templates.ts`, segnaposto `{{TT_TC}}`/`{{TTTC}}` sostituiti deterministicamente — RF-25, mai numeri nel prompt) + `subjectFor` (D1); EmailAdapter (`src/channel/email-adapter/`: imap-client con internaldate ADR-001 e senza flag in lettura, smtp-client, message-router con normalizzazione identità, `EmailAdapter.markSeen` — D7) con nuove dipendenze `imapflow`/`nodemailer`/`mailparser`; wiring `src/channel/email-processor.ts` + CLI `channel:email:fetch/process/send`, `llm:parse`, `llm:generate`; helper `src/cli/email-wiring.ts` che inietta le componenti reali nei comandi `round:*`/`tournament:*` (problema M). Correzioni di spec della tabella §7 applicate a LLD/plan/PRD (subject, interfaccia Parser, LLMError, segnaposto TT/TC, `auto_registered`, criteri router, flag \Seen, round corrente, fuso Europe/Rome, mailparser nello stack, albero §5). 237 test verdi (69 nuovi: contract LLM con fetch mockato — CS7; contract per ogni tipo email; unit router/client IMAP/SMTP/adapter; e2e wiring con DB reale + fake ai confini: CS1 simulato, CL2/CL5/RF-24/RF-27/RF-31, stop batch su LLMError), typecheck e lint puliti.
- `2026-08-14T18:15:00Z` — Preparata l'analisi delle Fasi 5 e 6: prodotto `tasks/briefing-fasi/briefing-fase-5-6.md` (verifica di spec su plan/PRD/HLD/LLD e stato reale di `src/`; 9 decisioni proposte D1–D9: subject email, interfaccia parser con lista canonica, LLMError vs null, segnaposto TT/TC, nuovo EmailType `auto_registered`, classificazione Message Router, flag IMAP \Seen, round corrente del wiring, fuso Europe/Rome; nuove dipendenze imapflow/nodemailer/mailparser; tabella correzioni documenti e prompt per l'implementatore).
